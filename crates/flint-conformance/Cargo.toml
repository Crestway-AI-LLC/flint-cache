[package]
name = "flint-conformance"
description = "Conformance oracle: runs compatibility cases against any RESP endpoint"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
flint-resp = { path = "../flint-resp" }

[lints]
workspace = true
