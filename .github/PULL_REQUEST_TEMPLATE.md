<!--
Security fixes do not start as pull requests — see SECURITY.md.
-->

## What this changes, and why

## How it was verified

<!-- CONTRIBUTING.md explains the standard this project holds itself to, and
the short version is: a check that cannot fail proves nothing.

  tools/gates.sh          the whole gate; its exit status is the answer

If you added or changed a check, say what makes it able to fail — the case you
ran that made it go red, not only the case that made it green. If you could not
run the gate, say that plainly; an unverified change described as unverified is
much easier to review than one that is quiet about it. -->

## Anything you decided not to do

<!-- Scope you deliberately left out, an approach you rejected, or a case you
know is not handled yet. This section is a feature, not an admission. -->
