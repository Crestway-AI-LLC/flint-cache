# Bug write-ups

One file per bug that was worth explaining rather than just fixing: the
symptom, the wrong conclusion that was drawn first, the root cause, and the
check that now holds it. They are kept because the misdiagnosis is usually
the expensive part, and a fix without it teaches nothing.

## How a number is allocated, and why collisions are tolerated

**Take the next free number on `origin/main` and push as soon as your gate is
green.** There is no allocator and there is deliberately not going to be one.

Two sessions filing on the same afternoon will sometimes take the same number.
That happened **six times on 2026-09-06** — 0107, 0111, 0112, 0113, 0116, and
one more the same evening — and every one cost a rename: the file, the H1, the
index row, any inbound links, and the commit message. The gate catches it
(`bug number(s) naming more than one file`) or the rebase does, so a collision
is never published.

**Reviewed and kept, 2026-09-06.** Allocate optimistically, detect on merge.
The alternatives are worse in the ways that matter: a shared allocator is a
new thing two repositories must agree on and that can be down when you are
trying to write a file, and reserved per-session ranges leave gaps that make
the series harder to read for everyone who was not there. A rename is
mechanical and takes a minute; neither alternative is.

**Most of the exposure is self-inflicted and now closed.** Every one of those
six collisions happened while a branch sat un-pushed through a 20-plus-minute
gate run. The window that matters is between the gate going green and the
push, and it is entirely within one session's control. Batch the work, gate
once, then push before starting the next thing.

## About the `#NNN` references in comments

Code and commit messages across this repo cite bugs as `#118`, `#133`,
`#140` and so on. Those are ids in Crestway AI's internal work tracker, not
GitHub issue numbers and not links — they predate this repository being
public, and most have no write-up here.

Treat them as provenance, not as a lookup: a comment saying "(#133)" is
telling you the line exists because of a specific real incident, and the
comment itself carries what you need. Where a bug earned a full write-up,
it is a file in this directory and the comment points at the path instead.

New references should use the path, so a reader can follow them.

| Bug | What it is about | Detail |
|---|---|---|
| `0001-fullsync-race-under-load.md` | promotion of an unconverged replica loses data (RESOLVED 2026-07-13) |  |
| `0002-verify-ok-on-single-copy.md` | `verify` called a pair with a dead member healthy (RESOLVED 2026-08-06) |  |
| `0003-drill-port-overlap.md` | drills sharing ports adopt and kill each other's seats (RESOLVED 2026-08-06) |  |
| `0004-start-replaces-a-starting-seat.md` | `start` wiped a seat that was mid-sync (RESOLVED 2026-08-06) |  |
| `0005-oneshot-kills-its-own-seat.md` | a systemd oneshot killed the daemons it spawned (RESOLVED 2026-08-06) |  |
| `0006-silent-bootstrap-fakes-auth-failures.md` | a silent drill bootstrap reports WRONGPASS on a token it never created (RESOLVED 2026-08-07) |  |
| `0007-armed-clock-blames-replica-kill.md` | ledger judged old-master acks by the pre-kill clock; a replica kill got blamed for a master kill's loss (RESOLVED 2026-08-07) |  |
| `0008-cold-start-of-a-failed-over-pair.md` | cold start of a failed-over pair replicated nothing (RESOLVED 2026-08-08) |  |
| `0009-unknown-stage-passes-the-gate.md` | an unrecognised stage argument ran nothing and printed GATES PASSED (RESOLVED 2026-08-11) |  |
| `0010-drill-port-overlap-recurs-on-7411.md` | BUG-0003 again: 7411 claimed by two drills, so every gate exits FAILED (FIXED) |  |
| `0011-conformance-drill-cp-never-pongs.md` | `flintctl bootstrap` spawns the CP seat, then its own probe never gets PONG (OPEN) |  |
| `0012-walgap-livelock-retention-ignores-replicas.md` | WAL retention ignores replica progress, so a lagging replica can never catch up (FIXED AND NOW GATED 2026-08-21) |  |
| `0013-bulk-writes-stall-on-default-compaction.md` | compaction left at RocksDB defaults, so bulk ingest hits the write stall |  |
| `0014-chaos-unreadable-acked-loss-on-replica-kill.md` | chaos oracle fails an acked write on a REPLICA kill, intermittently; cause not established. CLOSED 2026-09-04 on Jeff's call as an INSTRUMENT ARTIFACT: the misclassification window is proven in code and the drill has not fired since the fix (CLOSED) |  |
| `0015-flintsync-probe-admits-a-cursor-the-wal-cannot-serve.md` | marked boot clears its own re-seed marker on a probe that skips the WAL-retention check, then crash-loops (RESOLVED) |  |
| `0016-retracted-du-is-not-the-dataset.md` | RETRACTED: `verify` was right; `du` on a data dir measures RocksDB's info LOG, not the dataset (RETRACTED 2026-08-18) |  |
| `0017-rocksdb-info-log-grows-without-bound.md` | 883 MB of RocksDB debug log against 248 KB of data, unbounded and fastest under replication churn. Bound pinned at 64 MiB x 5; the last stated gap — "the tests drive the helper, not the call sites" — was checked by deletion on 2026-09-05 and did not exist: `open_with_retention` fails without it, and at `open_read_only` a read-only open neither rotates nor prunes, so the bound there is inert and untestable by construction (FIXED) |  |
| `0018-upgrade-needs-the-new-cp-before-it-can-roll-masters.md` | `upgrade` rolls masters before the control plane, but a fenced promotion needs the NEW CP to understand `CPFENCE`; on any fleet whose CP predates it the one-command rollout stopped halfway with the pair demoted and drained (FIXED) |  |
| `0019-drill-root-on-a-mounted-volume-breaks-disk-pressure.md` | `FLINT_DRILL_ROOT` on a mounted volume fails `disk_pressure` at `hdiutil attach`, and the message blames the image (FIXED) |  |
| `0020-restart-drill-bypasses-fleet-sh.md` | `restart` declared no ports and cleaned up with a bare `pkill`, so it was invisible to the overlap preflight and leaked (FIXED) |  |
| `0021-gate-logs-overwrite-so-a-failing-run-erases-the-passing-one.md` | gate logs were per-step not per-run and the next run `rm -rf`'d the lot, so re-running to diagnose destroyed the evidence you were going to diff (FIXED) |  |
| `0022-rocksdb-tickers-read-zero-when-statistics-are-disabled.md` | PARTLY RETRACTED: the two stall counters are DB properties and were live all along, so BUG-0013 was never blocked; the real defect — `.unwrap_or(0)` discarding a readable "cannot answer" — is FIXED |  |
| `0023-chain-traversal-loses-a-link-but-only-inside-a-full-gate.md` | a chain link is nil after a master kill in a gate run; 5 solo runs never reproduce it. CAUSE FOUND 2026-08-20: the build pipelines 1000 SETs at a time and `Client::pipeline` read every reply and DISCARDED it, so `-THROTTLED` from the lag cap (BUG-0035) returned Ok — a refused write was indistinguishable from a stored one, and the traversal called it "a truly lost link". Both diagnosed instances lost exactly key `hop+1`, the frontier, which is where a refused build write lands and a replication loss would not. Instrument FIXED; no recorded instance is evidence of data loss (CAUSE FOUND) |  |
| `0024-cutover-reports-a-handoff-failure-that-did-not-happen.md` | a 5 s read timeout on the cutover handoff was reported as "source not disowned"; the source disowns anyway, and the same call on the freeze stranded a slot write-frozen (FIXED; trigger still unexplained) |  |
| `0025-recovery-completes-a-flip-onto-a-destination-that-never-imported.md` | the recovery controller read a destination's absent Importing record as "it owns the slot" and completed the flip, purging the source; fixed with an `Aborted` phase so an abandoned import and a completed flip are no longer the same bytes (FIXED) |  |
| `0026-a-disk-guard-edit-spliced-df-into-six-pipelines-and-killed-a-check.md` | a disk-guard edit pasted `df -h` into six `| sed` pipelines, so every failing step reported disk usage as its reason and one preflight became unable to fail (FIXED) |
| `0027-the-gate-leak-check-kills-other-sessions-fleets.md` | the gate's leak check was an unscoped `pgrep`, so it blamed another session's fleet on an innocent drill and `kill -9`'d it — four times in one run, seconds after `fleet_guard` had correctly refused to touch the same processes (FIXED) |  |
| `0028-drills-ignore-their-own-build-exit-status.md` | 26 of 54 drills ran `cargo build` without checking it, so a standalone run silently tested whatever was already in `target/release` — masked under the gate, which pre-builds, and the reason a broken build line survived for months (FIXED) |  |
| `0029-cert-reload-census-counts-the-whole-machine.md` | `cert_reload_fleet`'s pid census was an unscoped `pgrep -f`, so it sampled the whole box and reported another session's fleet exiting as "pids changed — something restarted", a hot-reload regression that never happened; the same pattern also matches the agent processes themselves, so opening an editor mid-drill turned it red (FIXED) |  |
| `0030-write-deadline-positive-control-is-calibrated-to-one-machine.md` | `write_deadline`'s positive control drove a fixed 32-thread load, one or two above the ~30-in-flight threshold a 1ms deadline needs at 33µs/write — so arming is near a coin flip (measured 1 failure in 30 CI gate runs; 12/199/3059 refusals across three local runs), and it held main red with "nothing was refused", which reads as an unwired knob rather than a condition never created (FIXED) |  |
| `0031-needs-reseed-is-cleared-by-the-start-that-should-honour-it.md` | a replica marked for re-seed clears the marker on start, warm-rejoins into a WAL gap the master no longer retains, re-marks and exits — forever; the FATAL promises "the next start will full-sync" and the next start breaks it. Recovered only by wiping the data dir by hand. Fixed by treating a retained span that begins PAST the cursor as a gap; bracketed by one frozen acceptance file run against two bundles — red on rc.56, green on rc.57 — and shipped in rc.57 (FIXED) |  |
| `0032-flintctl-start-asserts-pair-0-is-the-master.md` | `start` asserts the inventory's FIRST member PONGs and calls it "master", but after a failover that member is the replica — so a down replica panics the whole start path and disables `flint-supervise`, whose job is restarting it. `start` now requires only that SOME member answers (FIXED AND NOW GATED by `start_first_member_down_drill.sh` — the fourth construction; the three that failed are recorded in the file) |  |
| `0033-wal-retention-window-cannot-be-bounded-for-tests.md` | WAL retention is RocksDB's `wal_ttl_seconds`/`wal_size_limit_mb`, fixed at 6h/8GiB by `RocksKv::open` and reachable by no flag — `open_with_retention` is the seam and has no caller. Not a testability blocker after all: the boundary is reached by RETIRING segments (`FLINTSNAPSHOT` then deleting `archive/*.log`), in-process for the narrow admission case and now in release acceptance for the coarse one. The flags remain worth having so a test need not depend on RocksDB's directory layout (FIXED) |  |
| `0034-flint-server-help-starts-a-node.md` | `flint-server --help` was ignored like any unrecognised flag, so it bound the default port 6380 and served forever instead of printing usage; one stray copy sat outside every drill's scope and made `fleet_guard` refuse 64 drills in one gate (`--help`/`--version` FIXED; general rejection was implemented, hung `slot_map` for 4:45 on a `--advertise` the server never read, and was REVERTED — the callee's flag set was enumerable, the callers' was not) |  |
| `0035-the-default-lag-cap-sheds-under-gate-load.md` | gate 21 shed 20328 of 50500 writes at the SHIPPED `--lag-hard-ms 1000` with no induced stall, falsifying `lag_cap_drill.sh`'s "a 1000ms cap is simply unreachable under any load the harness generates" and `slo.md`'s no-stall/0-shed row; separately `repl_drill` aborts at the load step under `pipefail`, so `FAIL repl` reports a verdict for six assertions it never reached. Not reproduced in 4 standalone attempts, one with CPU and disk both pegged; the fresh-binary/Gatekeeper explanation is dead (build.log: nothing was rebuilt). Both drills now report shed writes instead of misreading them — `controller` shed 2428 on a quiet box and passed where it would have cried "tail lost" (drills FIXED; the shed itself OPEN) | **(RESOLVED 2026-09-05 — and the file contradicted itself for twelve days.)** Its header said "still open: BUG-0038's fix" while its own body said of that fix "DONE, and it was better than predicted" -- 631ms to 115ms operating point, `writes_delayed_soft` to zero, ~885ms of margin under the shipped caps, landed 2026-08-20, FOUR DAYS BEFORE the re-scope that called it open. The whole argument was that the 500ms soft cap sat BELOW a 631ms ordinary operating point, so healthy traffic lived inside the brake; that is no longer true and the design question retires with it. The residual -- gate 21's unexplained perturbation -- is now MEASURED rather than explained: classifying all 121 gate failures across 609 main runs finds `repl` in exactly one, on 2026-08-09, eleven days BEFORE gate 21 and before the fix. **427 main-gate runs since the fix, zero `repl` failures.** That is a rate and not a cause, deliberately: nothing establishes what perturbed gate 21, and naming the shipper would assert what no check showed (ADR-0028). Caveats kept because a clean number invites over-reading -- the sample is MAIN, and gate 21 ran on a branch; `roll_shed_drill` (2026-08-22) now covers the shape so a recurrence has somewhere to be caught. RESOLVED not FIXED: nothing was fixed here today, the fix was BUG-0038's in August |
| `0036-the-sibling-guard-cannot-see-a-sibling-test-run.md` | `fleet_guard` refuses when a sibling Flint project has a fleet up — the check written precisely so contention is not misread as flakiness — but `_fleet_sibling` matched on the `flint-<project>-<component>` BASENAME, which a sibling's test binaries (`cold-modify`, `ttl-ccba…`) never carry, so a whole `cargo test` sweep read as an empty box across four gates. Anchored on the cargo target path instead (FIXED) |  |
| `0037-repl-catch-up-waits-on-the-wrong-key.md` | `repl`'s catch-up loop waited on the last STRING key while the load writes 500 hashes after it, so "caught up" was declared before the hash the parity check reads had replicated; passed for months on an idle box and reports `hash mismatch ('v250' vs '')` under contention, which reads as replication losing a write. Waits on a sentinel written last (FIXED) |  |
| `0038-the-replication-shipper-runs-at-a-quarter-of-its-documented-rate.md` | the FLINTSYNC push loop is commented "~200MB/s, far above a link" and measures ~50 MiB/s, so the shipped `--lag-soft-ms 500` sits BELOW the ordinary operating point and the brake is engaged during healthy traffic; fixing the coalesced acks took a quiet-box pair from 615ms to 115ms peak lag (FIXED) |  |
| `0039-ci-floats-on-stable-so-a-rust-release-reds-the-repo.md` | CI installs `rust-toolchain@stable` and runs `-D warnings`, so a new Rust release reds the repo with no commit here; a 1.98 lint failed three consecutive commits that never touched the file, and local clippy (1.96) could not see it — not "did not", cannot. Occurrence fixed; the toolchain is PINNED in `rust-toolchain.toml`, and the MSRV was measured the same night (`907f5f35`) — 1.85 was FALSE, three dependencies require 1.88 and cargo refuses at resolution, so our own code had never been compiled at the published minimum. `msrv.yml` verifies the declaration per push and `tools/gates.sh msrv` adds the `rocks` config it does not build. This row and the write-up's Status both said OPEN for two weeks after the fix (FIXED) |  |
| BUG-0040 | the proxy cannot say which node served anything | One global AtomicU64 for pool commands and no request counter on nodes, so "did traffic reach node X" is unanswerable — and the naive termination gate built on it passes automatically for every dead host (FIXED 2026-08-21, proxy 8cf94e7 + agent ce48e96; not yet deployed) |
| BUG-0041 | a write in flight at master loss outlives the retry budget (FIXED 2026-09-05) | The zero-client-errors contract broke through a 5s masterless window while every structural assert passed -- a real product violation, not a flaky test. **CAUSE CONFIRMED 2026-09-05, and the title it had was refuted: nothing escaped the budget, the budget was SPENT.** Both surviving occurrences recorded `reply at +5.02s after 0 acked '-ERR no reachable master for this slot'` -- `+5.02s` is `RETRY_BUDGET` exactly (`flint-proxy/src/main.rs:67`), every error exit in the retry loop is guarded by `now > deadline`, and that string is the proxy's own, emitted when rediscovery never found a target. `after 0 acked` is the writer's FIRST write, the only one that spans the whole outage: the writer is serial and a `-ERR` does not break its loop, so the next write starts with a fresh 5s budget against a window nearly over. Exactly one error, always the first. SEEN THREE TIMES, not once (08-22, 08-25, 08-29) -- found by classifying all 125 ops-gate failures across 988 runs; the earlier count was never revisited. The margin was never positive: the agent's own journal puts recommendation-to-execution at 4,290 and 4,603 ms, BEFORE detection, against a 5s budget -- **the masterless window and the retry budget are the same size by construction, and which side of the line a run lands on is noise.** 3-in-960 is this DRILL's rate of having a write in the bad window, not customer exposure; the product statement needs no rate. NOT the separate `-LOADING` passthrough gap: both replies are the proxy's own no-master string. **CORRECTED 2026-09-05: no SLO says "zero".** This row and the file quoted `docs/slo.md` as promising "zero client-visible errors"; inside THIS repo that path is a different document saying a client "**typically** sees no error at all", which one error in ~1,400 writes does not contradict. The quoted sentence is real but lives in the managed-plane repo's slo.md, where it REPORTS two measured runs (30 kills per setting on 7 hosts) rather than promising anything -- a measurement that came out zero is not a guarantee of zero. Same cross-repo shape as BUG-0101, one document over. The mechanism is unchanged and still real; what changes is that the drill's `ERRS == 0` is stricter than anything published, so the argument "the assert is a product claim" does not hold as written. **FIXED 2026-09-05: `RETRY_BUDGET` 5s -> 10s** (Jeff's call). Five was the same size as the window it had to outlast; ten covers it with ~4.6s of margin. **And ten is not arbitrary -- it is the RTO budget.** slo.md states "Failover time (RTO) -- Budget: 10 s", and the proxy, whose job is to absorb a failover so a client sees a latency spike rather than an error, was giving up at HALF the time the product allows that failover to take. The two now match, so the contract is self-consistent. That argument was found AFTER the change, by reading slo.md for what the change would affect rather than for what would justify it. Chosen over shortening the window (which means cutting Tier 2's re-confirm streak, there to stop a promotion firing on a blip) and over relaxing the drill's `ERRS == 0` (the only place the behaviour is visible) -- both spend a safety property, this spends the time a client waits before being told a cluster is genuinely dead. It helps READS too: a replica self-fences stale reads at 3s (`--replica-read-stale-ms`), shorter than the window, so a read arriving after the fence fell back to a master that did not exist yet and exhausted the budget exactly like a write. Widening THAT fence is a separate decision about a promised staleness bound and was not bundled. Test costs six seconds and says so in itself: a wall-clock deadline has no honest cheap test, and reverting the constant reproduces the production error verbatim -- `not absorbed after 5.017703666s: "ERR no reachable master for this slot"`, the same string and timing as the 08-25 and 08-29 occurrences. Three weeks at "cause unknown", now reproducible in six seconds without a fleet |
| BUG-0042 | `controller_ha` only tests its invariant on a fast box | Three controllers race to promote; on 8 cores the race never starts (`fenced attempts: 0` in 4 of 4 runs) so the drill passes without exercising the property, and on a 16-vCPU box where it did start, a second real promotion landed at a higher epoch — the invariant ADR-0004's no-per-group-Raft argument rests on. The green history is evidence the race rarely starts, not that the fence works (OPEN) |
| BUG-0043 | `FLINTCONFIG lag-hard-ms` reports success and applies a different value | the command acknowledges the value it was given and stores another, so an operator who tunes the hard cap gets a silent substitution and a config read that agrees with neither (FIXED 2026-08-22, same day) |
| BUG-0044 | the disk guard samples on a fixed clock, so a fast writer outruns it | sampling on a timer rather than on headroom means a writer can cross the threshold and reach ENOSPC between two samples — the guard exists precisely to make that impossible (FIXED 2026-08-24; the branch audit it grew into CLOSED 2026-08-25) |
| BUG-0045 | the convergence gate is an equality, so it never fires under write load | a pair under sustained writes never satisfies an exact-match convergence test, because the target moves between the two reads; the gate passes by never being reached (FIXED 2026-08-24) |
| BUG-0046 | managed redundancy repair wipes the replica it is waiting for | on `--manage-slots`, a full sync slower than ~20s is restarted forever: the controller wipes and retries the transfer it is waiting on, so at fleet dataset sizes it is the guaranteed outcome, not a risk (FIXED 2026-08-24 by #176; confirmed + drilled 2026-08-26) |
| BUG-0047 | `failover_drill` is not parallel-safe, and it is not the OOM killer | the drill starts one seat outside fleet.sh's tracking and declared a scope name it does not use, so its zombie had no marker beyond a port and died under `FLINT_GATE_JOBS>1`; the OOM-killer explanation was measured and refuted (peak 792MB of 15702MB) (FIXED 2026-08-24) |
| BUG-0048 | `process::exit` from the tailer thread races RocksDB's static teardown | the WALGAP path diagnoses correctly and writes the re-seed marker, then segfaults on the way out: `process::exit` runs C++ static destructors while the serving path, the disk sampler and RocksDB's own compaction threads are still live. Promises `exit 3`, delivers 139 under load. All six sites audited: only the tailer was exposed, the five startup ones keep process::exit. Fixed by hard_exit() — flush, then _exit, so the C++ handlers never run (FIXED 2026-08-26, b899cf7) |
| BUG-0049 | `lag_cap` waits for ambient slowness to produce its own precondition | the drill hopes a write burst outruns replication rather than forcing it, so on a run where replication keeps up nothing sheds and it correctly reports the RPO mechanism unproven — a proxy standing in for a property, the same shape as the #176 waits one level up. Reds main at random; three fix options weighed, and the obvious one converts it into a different experiment. FIXED 2026-08-26 in `fcc0028` by forcing the lag with `--stall-replica-ms 200` rather than awaiting it — 70 writes shed at the 5 ms cap where the old form shed 0 (FIXED) |
| BUG-0050 | a replication cursor resting INSIDE a multi-key batch can never advance (FIXED 2026-08-26) | RocksDB advances the sequence per KEY but iterates per BATCH, so an 8-key write leaves 7 sequences no batch starts at. `get_updates_since` on one of those skips the containing batch, yields nothing, and repl.rs:178 reads empty as "WAL recycled" — a healthy replica is told to full-sync with the data one segment away. Reproduced deterministically: cursor at batch start-1 SERVES, cursor one later REFUSES. 18 occurrences on the playground. NOT retention — raised twice for this, recurred both times (OPEN, mechanism confirmed) |
| BUG-0051 | `fleet_kill`'s wait-for-death loop reads a list nothing fills | the pid list the loop iterates is appended to by `fleet_signal`, a DIFFERENT function that sends SIGSTOP/SIGCONT and has no wait block; `fleet_kill` declares its own `local _killed_pids=""` and never appends, so the block is unreachable. Confirmed by running it under `bash -x` with a positive control on the kill path: `kill -9 <pid>` present, `kill -0` absent. The protection is #176's: `kill -9` is delivery, not death, and 21 drills respawn on the same ports, so the documented symptom is `promote_notice`'s "nothing listening on 127.0.0.1:6911 after 30s". A wait that never happens is indistinguishable from one that finished instantly, which is why it was filed before it was fixed: a bug of that shape must not be closed by adding code nothing witnesses. FIXED 2026-08-26 — the append is in `fleet_kill`, and `kill_release_drill.sh` witnesses it (FIXED) |
| BUG-0052 | the re-probe debounce is shorter than the probe it debounces | `REDISCOVER_DEBOUNCE` is 250 ms; `discover_master`'s read timeout is 800 ms **per node** and it walks both pair members. The gate is stamped before the probe and the lock dropped, so it bounds probe STARTS, not probes in flight: over an outage a herd is re-issued about every 250 ms, each racing a write into `routing.masters` — the same `addr -> none -> addr` flap the debounce exists to prevent, at reduced rate. Weakest exactly when load-bearing, since a slow probe means a node that binds and answers nothing (#176's shape). The existing test cannot see it: its address resolves to nothing, so all 32 iterations finish in microseconds and never leave the window. FIXED 2026-08-26 — single-flight per address, with the 250 ms window demoted to a quiet period AFTER a probe rather than a bound on how often one may start (FIXED) |
| BUG-0053 | MGET and MSET have no cross-slot guard, and answer wrongly instead | the node refuses cross-slot SINTER/SUNION/SDIFF because "a key this node does not own reads as an empty set ... NOTHING UPSTREAM CATCHES THIS". That reasoning transfers verbatim to MGET, which reaches neither the shared crossslot helper nor the inline one: a key on another pair returns nil IN ITS CORRECT POSITION, indistinguishable from absence. MSET is worse — it writes the phantom onto a node that does not own the slot. The proxy cannot catch it by design (`route_key` is the first key). Confirmed independently from the source by the S3-accelerator session. FIXED 2026-08-26 — MGET and MSET now call the same `crossslot` helper the set ops use, unit-tested against MemKv rather than needing the two-pair fleet the symptom does (FIXED) |
| BUG-0054 | `promote_notice`'s negative control subtracts two spawn-dominated samples | asserted `SLOW_DELTA >= 30` against a delta formed from two INDEPENDENT `valkey-cli` spawns, where spawn dominates — 37 ms local, 62 ms on the 4-vCPU runner. Unloaded the spread is 6 ms against a 50 ms signal; loaded it reaches the size of the signal, and the check fails on a machine rather than a regression. Two diagnoses of mine died first: a two-handler race (six runs, tight cluster, no bimodality) and independent discovery on a slow box (a 2 s pause changes nothing). (FIXED 2026-08-26 — both assertions now key on the proxy's re-probe log line, each verified against a world where its mechanism is absent) |
| BUG-0055 | `forward` sleeps 50 ms after it has already found the new master | two `-READONLY` handlers disagreed: `forward_collect` retried at once, `forward` slept 50 ms unconditionally, so the cost of a controlled failover depended on which serviced the write. Deleting the sleep would be wrong — a backoff earns its place when the re-probe found nothing. What neither did was ask which case it was in. (FIXED 2026-08-26 via `Topology::still_master`; measured 49-64 ms -> 1 ms, and it could not have landed before BUG-0054, because the drill was asserting the defect's magnitude) |
| BUG-0056 | the release gate runs ~130 drill scripts and checks that none of them parse | no `bash -n`, no `shellcheck`, no `shfmt` anywhere in `tools/gates.sh`, so a syntax error in a rarely-run branch surfaces as a confusing runtime failure at the end of a long gate rather than in the first second. The same class of bug cost 48 minutes of an idle EC2 box in the ops repo the same day, where a generated script failed to PARSE and the caller rendered the missing exit status as "the run may still be going". The trap in the fix is recorded with it: the stage must assert its file list is NON-EMPTY, or a glob that stops matching produces "checked nothing" and reads as a pass. FIXED 2026-08-27, extended 2026-09-05 (FIXED) |
| BUG-0057 | the tier client is per-FileSystem, so disabling the FS cache exhausts Netty | `FlintS3AFileSystem.initialize` built a `RedisClient` -- a Netty event loop group with a thread per core -- and an `S3AsyncClient` PER INSTANCE. Normally once per bucket; under `fs.s3a.impl.disable.cache=true` Hadoop builds a FileSystem per `get()` and Spark never closes the ones it opens for `read.parquet`, so it ran per READ. After ~30 table reads: `IllegalStateException: failed to create a child event loop`, with `ulimit -n` 65535, so not a tight limit. First conclusion was that we never close the tier -- we do, at `FlintS3AFileSystem.java:192`; nothing calls it. (FIXED — connections pooled and reference-counted: `RedisClient` by tier URI, `S3AsyncClient` by endpoint/region/path-style/CREDENTIALS, since that client carries its provider and handing one tenant's to another's mount is worse than not caching. **The bigger version of this fix was tried and REJECTED.** Pooling the whole `TierSupport` by a config signature gave +4 threads for twelve mounts instead of +26 — and made two mounts share one AAL `S3SeekableInputStreamFactory` and its in-memory object cache, so the Iceberg suite read **0 chunks where it expects 16** and the Hadoop contract suite failed `testSeekBigFile` with **`EOF End of file reached before reading fully`**. That is D12.29: a stale LENGTH is worse than a stale value because it presents as truncation. Sharing therefore stops at the connection, which is the resource that actually ran out. Gated as `TierSharingSuite`: asserts the connection counters directly, and asserts each mount keeps its OWN factory so the rejected version stays rejected. **Found only by running the full gate** — 108 individual checks had passed while the two suites that catch shared-cache staleness were never run) |
| BUG-0058 | a tier that is already down fails the job at FileSystem init | ADR-0023 D12.9 calls degradation "the property that decides deployability": *any failure falls through to the origin and the read still succeeds*. Measured by a deliberate control -- stop `flint-server`, run the cached arm -- and **not one query ran**: `Failed to initialize filesystem s3a://...: RedisConnectionException: Unable to connect`, `Caused by: java.net.ConnectException: Connection refused`, repeating per table until the job aborted. `FlintS3AFileSystem.initialize()` calls `TierSupport.build(...)`, which opens the Lettuce connection EAGERLY; the exception escapes `initialize()` and Hadoop cannot hand back a FileSystem that failed to build. D12.9's mitigations (`REJECT_COMMANDS`, the latency budget) are all on the READ path -- they protect a tier that dies AFTER a working connection exists, and nothing covers one already down when the FileSystem is built, because the connect happens before there is a read to fall through from. **Explicitly NOT claimed broken:** the mid-job kill D12.9 was written for; this run says nothing about it. (FIXED — `TierSupport.build` installs `LazyTierCommands` on a failed initial connect: a proxy over `RedisAsyncCommands` whose every call throws until the tier answers, retrying at most once per `flint.tier.reconnect.ms`. Adds NO new degradation path — a `RuntimeException` from a tier call is what `FlintObjectClient.guard` already catches inline and degrades on, so the outage takes the fall-through that was already tested. Proxied, not reimplemented: an enumerated subset of a several-hundred-method interface works until the first unlisted method, which is how the allowlist-by-case-label was a bug once already. Gated as `TierDownSuite`, 10 checks, which requires not just that reads work but that the client TRIED (`tierFailures > 0`), that it did not try too hard (attempts held at 2 across 6 reads), and that it RECOVERS when the tier returns. **Positive control run:** reverted to rethrow, it fails at check 2 with the production symptom. Regression: 49 existing checks unchanged. Note `ResilienceSpike` covers the mid-job kill and **the gate never ran it**, which is how this reached a real Spark job) |
| BUG-0059 | the D12.9 resilience spike had been failing, and the gate never ran it | `ResilienceSpike` covers what ADR-0023 D12.9 calls the property that decides deployability, printed `RESILIENCE SPIKE FAILED` against a healthy tier and correct data, and **nothing read its result** -- `grep -rn ResilienceSpike --include='*.sh'` matched nothing in the repo. Confirmed failing at `HEAD` before any change. First conclusion was that a dead tier returned WRONG BYTES, since both failing checks were content checks; the data was fine and the expectation was stale -- `counting_s3.py` generates `md5("{key}:{gen}:{i}")` and `expected()` still computed `md5("{key}:{i}")`, `gen` having been added so an object could be mutated. Two further defects were latent because it never ran: it shelled `valkey-cli -p 9399 shutdown` while dialling whatever port it was GIVEN, so on any other port **it killed nothing and every check after the kill passed against a healthy tier**; and it assumed valkey, while **Flint implements no SHUTDOWN**, so under `FLINT_TIER_ENGINE=flint` the kill was a no-op. (FIXED — generation restored, kill routed through `Suite.killTier(conn)` which returns only once the port REFUSES, tier restarted before exit since `start_svcs` exits when it does not answer, and **gated** as "tier killed mid-job". The positive control is the history rather than a construction: correcting `expected()` turns it green without touching product code, which is what separates a stale test from a real defect. Full set green: 84 checks, 0 failures) |
| BUG-0060 | every resource limit is per-unit, and nothing bounds the aggregate | an audit, opened with its own inventory: ~two dozen named limits exist and each is defensible alone, but `MAX_QUERY_BUF` (1 GiB) is PER CONNECTION and `MAX_CONNS` is 2048, so the input this node will accept sums to 2 TiB on a 16 GiB seat. `OUT_FLUSH_THRESHOLD` is the likelier one to bite: 1 MiB of reply buffer per connection is ordinary, and 2048 ordinary connections is 2 GiB with no client misbehaving. The product of two defensible limits is defended by neither. FIXED 2026-09-05: whole-collection reads are costed against a measured 3.5x multiplier and bounded as a SUM, shipped off for a week while `--collection-read-mode observe` existed to price it, then defaulted ON at the ratified 25% (FIXED) |
| BUG-0061 | thirty drills kill the supervised seat before its supervisor | every cleanup of the form `fleet_kill server; fleet_kill controller` races the thing whose job is to respawn dead nodes. They have been winning because `fleet_kill` returned within milliseconds of `kill -9`, so the controller died before it could react — BUG-0051's fix makes it wait for the deaths, which is correct, and the window then opens to the length of the wait. `controller_multipair` and `snapshot_restore` leaked a respawned node on the first gate run carrying that fix, against ten clean runs the same day. 30 drills share the ordering; the other 28 differ only in how fast their controller notices. Fix is the drills, not fleet_kill: callers invoke it once per seat kind so the order lives in each drill, and a cleanup that kills managed nodes while their manager runs is wrong independently of timing. FIXED 2026-08-27, landed with BUG-0051 and gate-verified, with a source assertion so all 30 are covered deterministically rather than only when the race is lost (FIXED) |
| BUG-0062 | the probe clears a stale snapshot, then the attach races the WAL | A restarted replica rewound to a snapshot at seq 207196, `probe_resume` PASSED (the "tailing incrementally instead of a full re-seed" line only prints on Ok), and the tail then died: `WALGAP cursor 207196 is no longer reachable (oldest retained batch starts at 207198, past the 207197 needed)` — short by ONE batch. Not a stale-snapshot misjudgement but a **check-then-act race**: under live ingestion the master's WAL recycles between the probe answering and the tail asking, so the window `probe_resume` narrowed (added for soak run 34 cycle 6, `main.rs:443`) cannot be closed by narrowing it further. The recovery does not converge either: `main.rs:4795` marks NEEDS_RESEED and exits so "the next start will full-sync", but the next start **re-selected the same snapshot** — the evidence shows `snap-...-seq207196-e0.7` rewound to twice and fatal both times — re-running the identical probe and losing the identical race. That is the livelock `main.rs:4405` says "no supervisor can break", reached by a path the fence-refusal wording does not cover; the pair is left at live_replicas 0 and `flintctl` gives up at its deadline. **A retry that re-derives the same inputs is not a retry** — either the losing snapshot leaves the running set before the next boot chooses, or the WAL span is pinned across probe→attach. Secondary and separable: `flint-ctl:4892` waits a hardcoded 60 s while scale-cluster computes a re-seed-sized budget; raising it alone converts this into a slower failure, not a pass. (FIXED 2026-08-27 — quarantine: on WalPurged the tailer renames every snapshot at or below the unresumable cursor to `unresumable-snap-…`, which `parse_rewind_candidate` will not accept, so the next boot has no candidate to lose the race with and takes the full re-seed. **It does not stop the race** — it makes losing terminate instead of livelock. `seq <= cursor` not just the loser, because the WAL only advances so older snapshots are unresumable too; quarantining one at a time costs a boot and a reconvergence deadline each (this run held three). Renamed not deleted, which retires the "discards usually-good work" objection: the bytes stay, and they were never good for this purpose. Deliberately NOT another `why.contains(...)` — matching the marker's wording is what let this through. Mutation-checked twice: a no-op quarantine fails 4 tests, and `seq == cursor` also fails 4, so the scope decision is defended too. **DEMONSTRATED on a running pair** by `tools/walgap_quarantine_drill.sh`, which forces WalPurged rather than waiting for the race (the 60-min soak passed without ever entering the path): it proves its precondition on the wire — B answers `WALGAP cursor 1256 ... oldest retained batch starts at 3192` before A resumes — then observes the quarantine fire, the snapshot renamed not deleted, no `snap-` candidate left, and the restart re-seed and converge. The first attempt recycled nothing because retention archives nothing until the memtable is FLUSHED (`FLINT_WRITE_BUFFER_MB=1` is the load-bearing half), and was correctly read as a setup failure, not evidence. Seed `1787810654671675139`) |
| BUG-0063 | the sibling guard cannot see an orphan, so it tells you to wait forever | A `flint-kv-server` at **ppid 1, 0.0% CPU, 44 minutes** blocked every drill on the box with "Wait for that run to finish" — there was no run and no parent. `fleet_guard`'s FOREIGN branch already reports orphans (added after the 2026-08-20 five-hour refusal against orphaned writer subshells); the SIBLING branch never got that lesson, so it recurred where the first fix could not reach — OPS-0056's family. **The obvious patch is wrong and looks right**: `_fleet_foreign` emits `pid=,ppid=,args=` so its `$2 == 1` test is sound, but `_fleet_sibling` emits `pid=,args=`, where `$2` is the command — a copied test compares a path against 1, finds zero orphans forever, and reads as a working check (BUG-0013's W-Amp shape). Fixing it means widening `_fleet_sibling` to carry ppid, which three call sites consume. Narrower cause: an UNNAMED sibling is activity-sampled by `_fleet_sibling_settle` and waited out, but a NAMED one is presumed a live fleet and refused outright — and a named server binary is exactly what a crashed harness leaves behind. `_fleet_sibling_activity` already samples CPU, so the machinery to tell an idle orphan from a working fleet exists and is not consulted here; a process at 0.0% CPU contends for nothing, and contention is what the guard exists to prevent. Do NOT widen FLINT_DRILL_FORCE instead — the refusal was right, the reasoning was missing. (FIXED 2026-08-27 — `_fleet_sibling`/`_fleet_sibling_named` now emit `pid ppid argv` like `_fleet_foreign`, and a new `_fleet_sibling_named_live` (ppid != 1) replaces `_fleet_sibling_named` in the settle condition. **Orphanhood, not idleness, is the discriminator** — a named sibling with a live parent still refuses on sight, unmeasured, because a sleeping fake fleet is still a fleet and making idleness the test would collapse the contract arm B asserts. An orphaned one falls through to the same activity check its test binaries get: idle proceeds, busy still refuses and is told to be REMOVED rather than waited for. Controls are `fleet_guard_drill` arms H and I, pinning opposite directions, and H asserts its fake orphan really has ppid 1 so it cannot pass vacuously. Mutation-checked both ways, failing DIFFERENT arms: reverting fails H, dropping the condition fails B.) |
| BUG-0064 | `cold_start_roles` cannot say whether the replica was loading or absent | Failed 2 of the last 60 gate runs on main with identical text, `FAIL: live_replicas 0 after cold start — the pair is storing one copy`. Not BUG-0061's reorder: the 08-26 failure predates it and the text matches on both sides. The assertion polls `replicas_of` for 40s, and a replica still INSIDE its load counts as an absent one on the master — so a timing fault on a slow runner and BUG-0008's silent single-copy fleet (RESOLVED, severity high) produce the same line. The drill already knows the distinction: `role_decided` exists twelve lines above because since #176 a node answers FLINTINFO from inside its load. Learned for `role_of`, never carried to `replicas_of`, in one file — OPS-0037's shape, and OPS-0045's one layer over. Assertion deliberately UNCHANGED (widening the budget is how a BUG-0008 regression gets hidden); the failure now dumps role/loading/live_replicas/seq_lag per seat and states the reading rule, so the next firing says which it is. (OPEN in its original half — the next firing, now instrumented, is what closes it. The **release-blocking half is FIXED**: the `decommission` verify-after-topology race that reddened the rc.68 release gate, plus an audit of all 18 `verify`-calling drills that found no further instances. Also carries the gate-redness measurement, **re-measured 2026-09-04 while gating rc.69: 15% -> 9.1%** (5 of 55 completed runs). `promote_notice` and `cold_start_roles` are absent from that window entirely; `decommission` is fixed since. **`failover` is now the largest single source with no file of its own** — twice in two days, recorded rather than filed, and it earns a file on a third firing. At 9% a release-branch gate is a one-in-eleven coin flip on something unrelated to the release, and rc.68 lost that flip) |
| BUG-0065 | a freshly promoted master self-fenced against its own pair's stale lease | One occurrence: the canary drill's masters-last phase aborted on an unexpected SelfFenced from pair 0's new master, 79ms after pair 1's promotion was fenced (`ops CI 33112175958`; quiet runner, same tree family 57/0 on the gate box the same hour — a narrow race, not load). CPLEASE reads by member-vector CONTAINMENT and CPFENCE writes by EQUALITY; the scoping reads correct, so one sample cannot pick between a stale own-pair record (successor 6921) and cross-pair aliasing via silent duplicates (successor 6923). The successor decides in one line and the journal row carried a fixed string while stderr — per-seat, dead with the box — had the name. Instrument fixed: superseded_cause() puts the successor in the row, mutation-tested; the gate matches on kind so nothing changes in what aborts. SECOND FIRING 2026-08-28 (`ops CI 33148455433`, ops @ 82ddfcc, core @ cd1e3c8) and the instrument paid: the row read `lease superseded by 127.0.0.1:6921` — pair 0's OWN other member, the node the roll had just demoted — so it is (a), a stale own-pair record, not cross-pair aliasing. A second and stronger line agrees because it does not depend on reading the successor: pair 1 NEVER FENCED in that run (zero `pair 1: … demoted` lines; the abort came after pair 0's roll and before pair 1's), so (b)'s prerequisite did not exist. Scope is now one path — a pair's own fence updating durable state without shared.leases.entries following, leaving a containment-keyed read on the pre-promotion member. NOT caused by the core commit it landed on: canary passed on the gate box and locally on cd1e3c8, the fenced path never touches reply encoding, and the first firing was on a different core. The "15 green CI runs so this is new" reading was a search window too short to reach the first firing. Rate: 2, both in ops CI, both node 6920 — the pair whose master rolls FIRST — never on the gate box or locally. Cause narrowed to (a) by the second occurrence on 2026-08-28 and the path that makes (a) possible is now closed (FIXED in code, UNCONFIRMED against a live recurrence) |
| BUG-0066 | a declared config key that nothing reads | `fs.s3a.flint.max.object.bytes` was a public constant on `FlintStreamFactory`, listed in the README for "S3A (paths 1, 2)" and printed by the preflight script. Nothing read it: `bind()` enumerates its keys and then built the client from the SHORT constructor, which supplies defaults. Three more keys the README listed for this path had no constant at all. **No symptom** — an ignored setting behaves exactly like one left at its default, and every test used defaults. **The wrong conclusion first** was that the README overclaimed and the rows should be struck; in fact path 2 supports all four, because it maps `flint.*` to `fs.s3a.flint.*` by RULE, so the rows were right for path 2 and silently false for path 1 — striking them would have deleted working documented behaviour. This is the SAME defect `FlintS3AFileSystem` carries a comment about fifty lines away, written when `flint.cache.sse-kms` was found unreachable: "an allowlist fails closed, which is right, but it does so SILENTLY and one key at a time". Path 2 was fixed with a rule; path 1 kept the enumeration and the bug recurred in the class next door. Path 1 cannot use that rule — it builds the client directly rather than through `TierSupport` — so the enumeration stays and needs a check. **Held by** `S3aSuite` setting `max.part.bytes=1` on path 1 and asserting nothing caches, with a control that the same read uncapped does; asserted by EFFECT, since reading the field back would pass on a client that stored it and ignored it. Verified armed: fails with 2 chunk keys on the pre-fix wiring. (FIXED 2026-08-27 — the class of bug is detected for the key that has a test, not structurally prevented) |
| BUG-0067 | the tier-down fix never reached the path it was written for | A tier that is DOWN at job start fails the job outright on adoption path 1 — `FileSystem.get()` throws `Connection refused` out of `bind()` before a byte is read. That is D12.9, bounded degradation, "the property that decides deployability", absent on the path the preflight recommends FIRST. **The wrong conclusion first** was that the new probe was at fault for pointing at a closed port. It is the product: **BUG-0058 is this exact bug**, fixed weeks earlier — but the fix went into `TierSupport`, which paths 2 and 3 build through and path 1 does not, and `TierDownSuite` — the ten-check suite that exists for this property — builds through `TierSupport` too. The gate for the property and the fix for the property covered the same two paths and neither covered the third. Root cause is the same structural split that produced BUG-0066 in the same class: two adoption paths, two construction mechanisms, every fix landed twice and nothing saying when only one landing happened. **Fixed** by giving `bind()` the same fallback — dial once, on refusal use `LazyTierCommands` so rejected commands degrade to the origin — plus `fs.s3a.flint.tier.reconnect.ms`, which this path could not set. **Held by** `ConfigReachSuite`'s TIER_URI probe: a closed port must still return correct bytes AND cache nothing in the real tier, the second half being the control that the configured URI was the one used. **The lesson**: a suite that constructs its subject the way the FIXED path constructs it follows the fix around and never tests the path that was missed. Still uncovered: a tier that dies MID-JOB on path 1. (FIXED 2026-08-27) |
| BUG-0068 | back-to-back gate runs collide on the multipart drill's tier | Running the valkey and flint gate arms back to back, the valkey arm failed `multipart etag key shape` with `port 9400 in use -- refusing to adopt a tier this drill did not start`. The refusal is correct; whatever held 9400 is the question. **What made it expensive**: the first log opened showed 7 passed / 0 failed, because the second arm had already rotated `/tmp/gate_*.log` — the failing arm's log was in `/tmp/gate_prev/`. "A drill's exit code is not its oracle" is already in the field notes; this is the neighbour, *a drill's log is not necessarily this run's log*. **Evidence**: 9400 free immediately afterwards, so nothing leaked permanently; the valkey arm re-run alone with 9400 verified free passed 33/33, and the flint arm had already passed 33/33; nothing in the change binds 9400. **NOT established**: which process held it, or whether it was the preceding run's tier still shutting down. Reproducing means running both arms back to back and sampling `lsof -ti tcp:9400` at the refusal. Written down because the next person sees a stage fail whose own log says everything passed. **DIAGNOSED AND FIXED 2026-08-27**: both drills that own 9400 did `kill "$TIER_PID"` and returned — SIGTERM sent, nothing waited for — so the drill exited while its tier still held the port. BUG-0061's shape, which the original write-up guessed under "if it recurs". The refusal is correct and was NOT softened; the cleanup now TERMs, waits for the port to go quiet, escalates to KILL and waits again. The port going quiet is the proof, not the pid going away. Armed with a standalone test that also TIMES OUT on a port that stays held. (FIXED 2026-08-27) |
| BUG-0069 | eviction is per-tenant policy with only a per-seat switch | A namespace is declared evictable in exactly two places, both PER SEAT: --evictable-ns at start or FLINTCONFIG at runtime. flintctl has no reference to it, the fleet repo's packaging sets it nowhere, and there is no inventory key (a comment in flint-chaos lists "an inventory line" among the ways one may be declared — that is the refusal check being agnostic about how the flag arrives, not a path that exists, and it reads like one). So the POLICY is per tenant while the SWITCH is per seat, and nothing composes them. main.rs:923 already states the cost: a pair whose members disagree has one side reclaiming while the other fills to -QUOTA — divergent POLICY, not divergent decisions. EVICTABLE_AGREE detects it and FLINTINFO reports it, but a detector is not a guard: there is no deploy step for it to refuse at. Turning eviction on across a fleet today is per-seat FLINTCONFIG calls with correctness resting on remembering to make all of them. Work: flintctl passthrough, an inventory key, render-inventory in the fleet repo, and a deploy-time agreement check. Open question that makes it non-mechanical: should a mismatch at deploy REFUSE or WARN — refusing matches how this tree treats config that could put a wrong number in a durability claim, but eviction is opt-in and a half-applied rollout may be a state an operator is deliberately moving through. **FIXED 2026-08-28**: inventory key threaded into every spawned seat, `reload` pushing it to a running fleet, `verify` REFUSING a divergent pair by name, and `evictable_ns` in PAIR_KNOBS so drift after deploy is reported too. The open question was answered REFUSE WITH AN OVERRIDE, but the roll-grace half matters more than the flag: a member inside the roll window is HELD BACK rather than refused, exactly as PAIR_KNOBS drift is, because `roll` converges the sides one at a time and passes through a legitimate disagreement -- refusing there would fail the very roll that fixes it. `--allow-evictable-mismatch` carries a deliberate half-applied rollout and SAYS so rather than passing silently. Held by `evictable_agree_drill.sh` with a positive control that the key reached BOTH seats, mutation-checked failing DIFFERENT arms; gate 133 steps green, the count rising from 132 being itself the control that the drill was registered) (FIXED 2026-08-28) |
| BUG-0070 | ~~the rejoin probe scans the WAL~~ the CURSOR TRANSLATION walks it, so failover time grows with a master's uptime | One 60-min min-replicas=1 soak, sub-phase timed: probe 35 / 2,354 / 4,077 / 4,985 ms as the cursor moved 149,795 / 2.8M / 7.8M / 10.3M — ~500 ms per million sequences, 91% of the decision window and 63% of the whole outage by cycle 4. NOTHING ELSE GROWS: fence flat at 2-3 ms, restore and the unaccounted remainder flat after the first cycle, restart a ~860 ms constant. Not correlated with the fence gap, which was 8,988 sequences on the worst outage. Chain: probe_resume -> FLINTSYNC (the real tailer handshake) -> updates_since_budgeted(cursor, 1) -> RocksDB get_updates_since, which walks WAL files to position. The 1-byte budget bounds what comes BACK, not what it costs to FIND — so a yes/no question is answered by a scan as long as the retained WAL, and RTO becomes a function of UPTIME. Perversely the cost rises the more caught-up the replica is, since position is reached from the WAL floor. Prevention rather than mitigation: the master already publishes the retained range as counters (wal_min_acked_seq, wal_headroom_seq), so resumability is a comparison, not a scan; safe because the real sync re-checks and downgrades a stale yes to the re-seed that is already the fallback (OPEN — nothing changed; the fix's own prediction is that probe collapses to single-digit ms and stops tracking the cursor). **CORRECTED 2026-08-28**: the mechanism in this row was WRONG and the fix built on it (`cursor_within_wal_bounds`) changed fleet failover time by nothing and was REVERTED. `get_updates_since` is cheap at every start position (0.2-11.9 ms, flat, measured to 2.4 M sequences with 1 KiB values, idle and under load). The cost is `own_seq_for_upstream`, which iterates EVERY batch and EVERY op from sequence 0 to translate the upstream cursor into local sequence space, on the REWIND path, BEFORE the check that was optimised: ~0.48 us/sequence, 62 ms at 200 K rising to 1,157 ms at 2.4 M, extrapolating to ~7.2 s at the fleet's 15.04 M cursor against 6,902 ms measured -- 63% of a 10,969 ms blackout. Pointed at by `fence=2ms probe=6902ms` in ONE rejoin: both are round trips to the same master at the same instant, so connection, TLS and saturation were excluded without measuring them. FIXED by a sparse hint index (one entry per 10 k upstream sequences, under the non-replicating system prefix because own-sequence numbers are node-local): 201.9 ms vs 2,689 ms at 3.6 M, same answer. NOT constant -- the residual is RocksDB's seek to the start position, which no stride removes. The hint cannot corrupt a replica because `get_updates_since` SKIPS the batch at its start, so a hint landing on the answer returns the NEXT batch (a successful WRONG answer, which a fall-back-on-failure guard misses); the walk therefore verifies its first cursor-bearing batch is still below the target, and `a_hint_never_changes_the_answer` plants five adversarial hints to hold it.. **CONFIRMED ON A FLEET 2026-08-28**: probe 9 / 65 / 18 / 103 / 61 ms at cursors 285 K / 3.05 M / 8.05 M / 10.73 M / 15.70 M against 3,073 / 4,199 / 3,225 / 6,902 ms before -- bounded 9-103 ms across two orders of magnitude with NO relationship to cursor depth (8.05 M gave 18 ms while 3.05 M gave 65 ms). Stalls 1,435-1,975 ms over five cycles against 1,493-10,969 ms, whose fourth cycle breached the 10 s budget. The local fixture predicted the BROKEN path almost exactly (~7.2 s vs 6,902 ms at 15 M) and over-predicted growth on the FIXED one: a fixture calibrated against one code path is not thereby calibrated against the path replacing it) (FIXED 2026-08-28 by the hint index -- the OPEN earlier in this row is about `cursor_within_wal_bounds`, which was built on the wrong mechanism and reverted, and it stayed this row's last state marker for six days) |
| BUG-0071 | a full re-seed on rejoin holds writes shut for ~90 s at min-replicas=1 | Soak cycle 3, 5 hosts, `min-replicas-to-write=1`: **94,204 ms blackout against a 10,000 ms budget**. Detection and promotion were healthy (527 ms); everything after is the rejoin. The rejoining seat was the OLD MASTER — cursor 8,191,139 against a tip of 8,175,258, holding **15,881 sequences the survivor never had** — so it was superseded and the full re-seed was CORRECT; the oracle recorded zero corruption and zero acked-write loss. What it cost was availability: `full sync: received 103 files`, ~8.2 M sequences shipped to replace a copy diverging by 15,881 — five orders of magnitude more than the divergence. Pinned independently of any timer by three scheduled snapshots taken 30 s apart at an IDENTICAL `seq8175258`: the master accepted no writes for the whole window, so this was a closed write path, not a slow one. Clients saw `-THROTTLED` fast-failures, not stalls (worst single write held 481 ms against a 94 s ack gap). NOT BUG-0070 — that is a ~5 s scan inside the probe, this is a ~90 s transfer after it, and fixing one does not touch the other. NOT an artifact of the reverted `FLINTWALRANGE` build the soak ran: the shipped code refuses the same cursor at the promotion fence instead and still re-seeds, marginally faster for skipping the doomed `try_rewind`. The rewind path that exists to avoid this RAN AND FAILED, which is the finding: three snapshots were taken during the outage but all at `seq8175258`, on the wrong side of a branch point at ~8.19 M. Directions: rewind to the FENCE (bounded, local, smallest), snapshot AT promotion, or stop counting a re-seeding replica against `min-replicas` until it completes. At `min-replicas-to-write=0` this cycle would have been ~1 s — the documented durability trade, not a fix (OPEN — the bimodal split between incremental tail and full re-seed is one sample; that ratio decides whether the published RTO must quote this case). **RATIO MEASURED 2026-08-29, and it is NOT the number to publish**: 33 collected rejoin decisions across seven soaks split 28 incremental / 5 re-seed (85/15), but all five re-seeds give the SAME reason -- `no snapshot dir at /var/lib/flint/snaps/g0`, the COLD START before any snapshot exists -- and all come from one early soak. This bug's case, a SUPERSEDED copy where snapshots exist but every one is on the wrong side of the branch point, appears ZERO times in the collected set. Worse, the collection cannot see it: every failure branch in scale-cluster/run.sh ends in `die` and the collect_* calls sit AFTER them, so a cycle that FAILS never reaches the always-run files -- and a 94.2 s re-seed is exactly a failing cycle. The one observation lives only in the failure bundle and is absent from that soak's own boot-decisions file (OPS-0082). So n=1 still. What IS established: a healthy steady-state rejoin tails incrementally, 28 for 28 once a fleet has snapshots, so the exposure is narrow and real rather than routine, and a fix should be judged on the tail it removes rather than on a frequency nobody has measured **NARROWED 2026-08-29**: the evidence bundle has NO `snapshot N is past the fence; trying older` line for the failing case, so the candidate PASSED the fence, was restored, and was then refused by the master for sitting past its tip (8,191,139 vs 8,175,258) — two checks answering the same question disagreed and the cheaper one said yes. Excluded: a name/content mismatch (the filename records `kv.latest_seq()` and the probe uses `kv.latest_seq()` of the restored copy — same quantity); naive cross-space comparison (FenceBound's own doc states the rule and the design follows it); and a wedged snapshot timer (real, now BUG-0073, but it yields "no snapshot dir", which is the OTHER five rewind lines and every re-seed in the collected set — the cold start, not this). Open question, testable: did `promo_fence_bound` return `Bound(n>=8191139)`, meaning the fence row does not name the branch point in the asker's space, or `Unfenced`, which try_rewind may treat more permissively than the FLINTSYNC handler that refuses it. Experiment designed: a rewind_rejoin arm that makes the two sequence spaces genuinely diverge and asserts the fence rejects the ex-master's own snapshot, logging which FenceBound arm was taken since no current line reports it. NOT fixed yet — three answers imply three different fixes, and today twice produced a fix built on a wrong mechanism, one of them gated 133/133 green while moving its target number by nothing **CORRECTED 2026-08-29 — the one observation is from the REVERTED build, and this file's claim that it was not is wrong.** That claim assumed the shipped fence would refuse a cursor try_rewind's fence had just accepted; it would not, because they are the same query. The ordinary promotion records `record_promo_fence(kv, epoch, kv.last_applied())` — the UPSTREAM cursor, in the old master's space, exactly as FenceBound's rule requires — so comparing the ex-master's snapshot seq against it is like-for-like and passes CORRECTLY. `Unfenced` is excluded too: try_rewind maps it to None and skips. The shipped handler then runs fence -> `own_seq_for_upstream` TRANSLATION -> retention, and FLINTWALRANGE lacked that translation (the same defect that failed failover_bystander and got it reverted), so it compared an upstream-space cursor against the master's LOCAL WAL range. The observed wording is FLINTWALRANGE's; no shipped refusal matches it. So the 94.2 s re-seed came from code no longer in the tree, and this bug rests on ZERO observations against shipping code. Remaining route to a re-seed here is the translation failing ("cannot map upstream cursor") — different cause, different message, and now cheap to evaluate via BUG-0070's index. Stays open because a 94 s outage is worth proving absent rather than assuming, and the "does a re-seeding replica hold the write path shut" question is real regardless **CAUSE FOUND 2026-08-29 by experiment on current code.** It REPRODUCES: a master accepting 400 writes its replica never received, then killed and the survivor promoted, re-seeds. The mechanism is NOT broken — the same run with a snapshot taken BEFORE the divergence rewinds correctly (`clears the fence (302 <= 302)`, then `rewind attach: upstream cursor 302 maps to local seq 604`), so fence, fallback-to-older, translation and resume all work and the fence/tip hypothesis was the reverted build's artifact. The entire cause is whether a candidate at or below the branch point still EXISTS, and what removes it is `quarantine_unresumable`: on an unresumable WAL gap it permanently disqualifies every snapshot `<= last_applied` (BUG-0062's livelock fix). Re-running the working experiment with exactly that rename applied re-seeds again — confirmed. Chain: tail hits a gap at C → all snapshots <= C disqualified → a later promotion fences at or below C → try_rewind refuses each remaining candidate CORRECTLY and runs out → full re-seed → at min-replicas=1 the transfer IS the outage. The quarantine is scoped to one cursor but unbounded in time: it justifies itself on THIS master's retention, while after a promotion the tail is served by a different node with a different WAL. Fix deliberately not proposed yet; it now has an instrument (exp 3 must flip without breaking exp 2 or reopening BUG-0062). Kept: drill arm D (superseded ex-master + valid candidate must rewind; asserts the divergence was staged, mutation-checked) and an accepted-path fence log, since only rejections named a bound before **FIXED 2026-08-29**: `quarantine_unresumable` writes the disqualifying cursor into the name (`unresumable-c<cursor>-snap-…`) and `try_rewind` reconsiders such a snapshot when the current fence sits BELOW that cursor — we are asking to resume at a LOWER position than the one that failed, against a different lineage, since a fence row exists only because a promotion recorded one. TERMINATES: a refused re-admission re-quarantines at the restored copy's own cursor, at or below that snapshot's seq, so trying it again needs `bound < seq` while using it needs `seq <= bound`. The PURGE path is untouched — with no promotion there is no fence row, promo_fence_bound answers Unfenced, and the candidate is skipped before the re-admission test, so the breadth that buys a one-boot re-seed is unchanged. No retroactive re-admission: pre-fix names carry no cursor and do not parse. Measured four ways (quarantined at 500 under fence 302 → rewinds; at 100 → re-seeds; legacy name → re-seeds; unquarantined → rewinds). Held by drill arms E and F, mutation-checked failing DIFFERENT arms — dropping the `bound >= at` gate fails F (reopening BUG-0062's livelock is what F catches), restoring the pre-fix never-return fails E — plus a round-trip test that quarantines through the SHIPPING writer and parses the name it actually wrote, since asserting the parser against a hand-written string would pass while the writer drifted. STILL OPEN separately: whether a re-seeding replica should hold the write path shut at all, for which the Flint KV accept-but-don't-count design is the input (FIXED 2026-08-29) |
| BUG-0072 | `rotate-certs` breaks the box it cannot sign on | `rotate_certs` guarded on `ca.crt`; signing needs `ca.key`, and there are real boxes holding one and not the other — both Flint ops boxes carry `ca.crt` + `int.crt` + `int.key` so a read-only agent can dial the mesh, and deliberately no `ca.key` (verified by `ls` on `flint-ops`). Past that guard, `resign_leaves`' FIRST step overwrites the live `int.key` and succeeds, the signing step fails, and the `assert!` panics — leaving a fresh private key beside the previous certificate, which the TLS watchers hot-reload within ~2 s. The box loses its mesh identity and, having lost it, cannot dial the fleet to say so. **Reproduced with openssl in a temp dir** rather than on a live box: step 1 exit 0, step 2 exit 1, modulus mismatch. **Found** while reading the command to implement it as an autonomous repair — the ops repo's `agent-authority.md` had nominated `RotateCerts` as the best candidate on the grounds that a wrong rotation costs a certificate and no downtime. Both halves were wrong: the repair is FLEET-WIDE (one seat's expiring leaf re-signs every mesh and edge cert) and it cannot run where the agent runs. **The absent `tier2.rs` dispatch arm was the only thing preventing this.** **Fixed** in two halves that do different things, measured separately: STAGING (mint into `{d}/.rotate`, move into place only after all three pairs exist and both EKU asserts pass) prevents the damage — with the guards disabled the leaf key survives untouched — and also covers a full disk, a missing openssl, a bad edge SAN; the GUARD only makes the refusal legible. Bonus: the ADR-0010 D2 EKU asserts read the STAGED certs now, so a wrong-EKU mint is gated instead of post-mortemed. **Residual, stated**: the move is per-file, so a watcher polling between a leaf's key and its cert still sees a mismatched pair — two renames instead of seconds of openssl, next poll correct; closing it needs a single-bundle load format. **The check that would have passed for the wrong reason**: the drill first asserted the refusal with `grep -q "ca.key"`, which passes with the guard removed entirely because openssl echoes `-CAkey .../ca.key` in its own error — measured, not assumed. It greps the guard's own words now and asserts the staging property separately. (FIXED 2026-08-28) |
| BUG-0073 | a panicking snapshot thread stops that pair snapshotting, permanently and silently | A pair starts a snapshot only by swapping `snapshot_inflight` false->true, and the spawned thread cleared it as its LAST STATEMENT — so a panic anywhere above (call_slow's parse, journal_event's I/O, a poisoned lock) skips it, the flag stays true, and `swap(true)` never succeeds again: **that pair never snapshots again for the life of the process**. The ERROR path was already correct — a failed snapshot logs, resets and retries next interval — so reading the error handling gives no hint; only the unwind path leaks. Cost is not a missing metric: `try_rewind` needs a snapshot at or after the branch point, so without one the next rejoin takes the FULL RE-SEED, which is BUG-0071's 94.2 s write blackout against a 10 s budget. One panic converts every later failover for that pair into the worst case, invisibly — absent SnapshotTaken events look exactly like an interval that has not elapsed. NOT observed in the wild; filed because the failure is permanent, silent, and on the one path whose absence costs 94 s. Found by applying the Flint KV session's lesson (a mitigation that exists but never runs reads exactly like one that works — their `flush_due` was disabled by an error path). FIXED with a drop guard so the reset survives unwind; held by three tests including a control and an early-drop check, and MUTATION-CHECKED by making Drop a no-op (the exact pre-fix shape), which fails both on the message "this pair never snapshots again" — being shown to fail is the point, since their equivalent test passed against unfixed code on a favourable HashMap seed (FIXED 2026-08-29) |
| BUG-0074 | a min-replicas-to-write no failover can satisfy, accepted at deploy | After a failover the survivors are `members - 1` and one IS the new master, so live replicas = **`members - 2`**. On a TWO-member pair `min-replicas-to-write 1` is therefore not a trade that sometimes costs something — it is a GUARANTEED write outage on every failover until the dead seat rejoins, a rewind at best and a full re-seed at worst (BUG-0071 measured 94.2 s of one). The value itself is legitimate and the default 0 is right; `main.rs:1966` already argues "1 IS A REAL TRADE, NOT A STRICTLY SAFER SETTING". What was missing is anyone saying so AT DEPLOY: nothing in flintctl related min-replicas to pair size, the inventory value went straight to every seat, verify never looked, and the consequence arrived months later at the worst moment — BUG-0069's shape exactly. FIXED: verify refuses when a member's LIVE `min_replicas_to_write` exceeds `members - 2` (live, not inventory, because the value is hot-reloadable — the inventory is what was asked for, FLINTINFO is what is true), naming the pair, the seat, the arithmetic and three remedies including `widowed-grace-ms`, which is the bounded alternative and on by default. `--allow-blocking-min-replicas` carries a deliberate posture and says so. Held by `min_replicas_survivable_drill.sh`, four arms; the load-bearing one is that the SAME value on a THREE-member pair is ACCEPTED, since a failover there still leaves one replica — mutation-checked failing DIFFERENT arms, keying on the number (`mr == 0`) rather than survivability fails the three-member arm while disarming the refusal fails the two-member one. NOT covered: whether three-member pairs are SAFE — they bootstrap and replicate, but manifest.rs:403 says the promotion fence's induction assumes two members, so recommending three as the way to run min-replicas=1 needs that revisit first (FIXED 2026-08-29) |
| BUG-0075 | a third member vouches for a lineage it never witnessed, in another node's sequence space | `promo_fence_bound` returns the smallest fence row above the asker's epoch, trusting the comment's claim that such a row is "in the asker's space" without checking it. On a two-member pair it always is, because the surviving peer executes every promotion. Add a third — `flintctl add-replica` ships it, uncapped, for D7 read fan-out — and a member can be promoted having never witnessed the promotion before it: fence rows are system rows, so an INCREMENTAL rejoin never receives them, and only a checkpoint carries them. The minimum then skips the missing row and answers with one denominated in a different node's sequence space. Measured: an ex-master carrying 60 widowed writes asked for its bound, was told **5406** (the new master's cursor in the DEAD master's space) instead of **202** (its own), compared `262 <= 5406`, rewound instead of re-seeding, and served a write the surviving branch never had. It fires on the fast path only — a full sync would have copied the row and correctly refused — so every improvement that makes incremental rejoin more common makes this more likely. Two-member pairs, the bootstrap default, are unaffected and the repro's control arm proves it. (FIXED 2026-08-29 — each row now carries the epoch it supersedes and vouches only for that lineage; a POSITIVE control asserts a two-member rejoin below the fence still rewinds incrementally, since a fix that refused everything would have passed every other check.) |
| BUG-0076 | a surviving replica is never re-pointed, so a three-member pair loses a copy at every failover | A replica learns its master exactly ONCE, from `--replica-of` at startup; no `FLINT*` verb changes it at runtime. On a two-member pair that is invisible, because the only other member is the one `restart-node` brings back with `--replica-of <current master>` computed at restart -- the re-point is a side effect of the restart, and every survivor gets restarted. Add a third member and the one that was neither killed nor promoted is the one nobody restarts, so it dials the DEAD master forever (`Connection refused`, retried for the rest of the run). Every member stays UP with the right role and epoch; only `verify`'s copy count sees it: **SINGLE-COPY: every member up, but 1 of 2 streaming**. This defeats the reason the third member was proposed: BUG-0074's `members - 2` arithmetic assumes the survivor keeps streaming, so at the instant of promotion live replicas is **0, not 1**, the `min-replicas-to-write=1` gate shuts exactly as on a two-member pair (1,021 writes shed), and the pair then runs single-copy until an operator notices. Found by the three-member chaos run whose oracle passed cleanly -- 16 kills, 901k writes, zero corruption. (FIXED 2026-08-29 -- the controller now sends `FLINTFOLLOW <addr>` to every other member after it promotes; the tail address moved into a swappable link, and safety stays with the new master's FLINTSYNC attach guard, so re-pointing moves an ADDRESS and never a decision about lineage. Held by three_member_repoint_drill.sh, which asserts live_replicas 1 BEFORE the dead seat is restarted -- restarting first would hide the bug, since that restart carries its own re-point.) |
| BUG-0077 | a seat that exits for a re-seed is never restarted on a fleet | A replica re-pointed onto a new master (BUG-0076) presents its cursor, and the attach guard CORRECTLY refuses one sitting past the promotion fence -- that copy holds writes the surviving lineage never had. The seat then does what it is designed to do: write `NEEDS_RESEED` and exit, because an in-process re-seed would tear the DB handle out from under live readers, and because *"exiting is also the honest signal -- under systemd (Restart=on-failure) the next start re-seeds unattended"*. Fleet seats are spawned by `flintctl` as plain processes with no systemd behind them, so the next start never comes and the pair runs a member short until someone notices. Only visible once BUG-0076 landed: before it, the re-point never happened and the replica spun on a dead address instead -- the same fleet run before the fix has ZERO of these exits and the one after has them. A loud exit beats a silent spin, and on an unsupervised fleet both end a member short. Durability was never affected (oracle: 16 kills, 848k writes, zero corruption). (FIXED 2026-08-30 -- and the supervisor already EXISTED: supervise.sh + flint-supervise.timer were written after the playground sat single-copy for FIVE DAYS on this exact failure. They are installed and enabled by `first-boot.sh`, which chaos-cluster must disable so six hosts do not each bootstrap their own single-host cluster -- so avoiding that trap silently removes the supervisor, and the fleet built to have its seats killed is the only topology with none. The chaos run now does one supervise-equivalent `start` before the post-chaos verify, never inside the kill loop, and self-hosting.md §2b finally documents the case its reboot unit does not cover.) |
| BUG-0078 | bulk ingest is capped by the edge while the data plane idles | A 2 TB ingest sustained ~62 MB/s against a 150 MB/s cap with nothing busy anywhere, so the number was a property of the path rather than the product. Measured, not reasoned: the generator sat at 0.4% CPU per feeder; the master reported `write_service_us:17`, `write_inflight:0`, zero shed writes, no compaction pressure, and **1.07 of 16 cores**. The same loader (8 connections, pipeline depth 256) then got **5,030 writes/s per connection direct to the master over mTLS** versus **690 through the plaintext proxy edge** -- 7.3x fewer, with the proxy using **0.08 cores** and 19 idle threads. A 256-command batch costs ~370 ms through the edge against ~51 ms direct, so the edge charges LATENCY PER BATCH and only clients that never wait for replies escape it (`valkey-cli --pipe` manages ~3,600/s per connection). Offered more concurrency the master went 14,488 -> 54,852 seq/s, 3.8x, still at 16% CPU: the data plane was never the limit. Invisible to every dashboard -- no shed writes, no lag, no stalls, no errors. **(FIXED 2026-08-31 -- and it was never the edge.** The node never set `TCP_NODELAY` on an accepted socket. It reads 16 KiB at a time and answers each read, so a client pipelining more than that gets a small reply written while its own send is still in flight -- the case Nagle holds -- and the peer waits out its delayed-ACK timer: **a flat ~50 ms per round trip at every depth from 16 to 512**, the rate exactly linear in depth. The cliff tracks BYTES not commands (32 B values move it to depth 256, the same ~16 KiB), which is what ruled out every application-level story. The proxy looked guilty only because it issues ~16 KiB per backend hop; **the 5,030/s direct baseline was paying the same stall**, amortised over a larger batch. One line: direct depth-256 goes **5,081 -> 472,978 writes/s**, through the proxy **1,266 -> 79,762**. Both standing candidates were wrong -- a never-waiting client was SLOWER, and a pass holds 15 commands not 3-8; pass DURATION, not size, was the variable. Guarded two-armed by `pipeline_nodelay_drill.sh` (0.3 ms against 50.4 ms with the seam set). Re-measure any throughput quoted from a >16 KiB pipeline before trusting it.) |
| BUG-0079 | the WAL shed guard fires at twice the distance the archive keeps | Two 2 TB ingest runs stalled at the same point -- 163-165 GB, ~15 min into a 150-200 MB/s fill of 1 KB values. The replica died on `WALGAP full sync required: IO error ... archive/002256.log: No such file or directory`: the master deleted a WAL segment the link still needed. It exits for a re-seed by design, nothing restarts it (BUG-0077), the master goes widowed and sheds every write. The cause is a UNIT MISMATCH nothing reconciles: RocksDB prunes the archive at **8 GiB** (bytes) or 6 h (time), while the guard that protects replicas fires at **4,000,000 sequences**. Measured at failure: 4,327 bytes/sequence, so 8 GiB holds ~1,985,000 sequences and the guard fires **2.0x too late** -- `writes_shed_headroom` and `writes_shed_lag` sat at 0 all run while the archive cut the replica off. The guard is safe only while a sequence averages under 8 GiB/4M = 2,147 bytes. Not OOM (55 GB free), not disk (90 GB of 3.5 TB), not a stale iterator (`seq_lag` tracks the freshest ack, which with one replica is this one). Availability, not durability: nothing lost or corrupted. (OPEN -- derive the threshold from the archive budget, and pin retention to the slowest LIVE replica, whose cursor repl_hub already has. **2026-08-30:** the volume-derived budget shipped and was then found INERT on half a fleet -- the master read 32,768 (a 1 GiB archive) where its replica read 8,388,608 (256 GiB), same disk, same binary. A boot RACE: the budget is sampled from the data directory before the engine creates it, and `unwrap_or(0)` turned `ENOENT` into a zero-byte volume, clamping to the floor. Fixed by creating the directory before measuring it, and that reporting gap CLOSED 2026-09-02: FLINTINFO gained `wal_archive_mb` and `wal_archive_src`, so a floor-clamped seat contradicts its own `disk_total_bytes` and `wal_budget_drill.sh` in CORE asserts it. **2026-09-05:** what remains is narrower and is NOT the mount ordering, which `flint-nvme.service` has enforced since 2026-07-21 (`Before=flint-first-boot`, which `Requires=` it) -- `nvme-mount.sh` `exit 0`s when no device matches the MODEL, so "mounted the store" and "found none" are the same success and the data lands on the root volume; only `chaos-cluster/up.sh` checks it at bring-up.) |
| BUG-0080 | a queued write never bumps the watch table, so EXEC commits when it must abort | `WatchedKv` bumps the watch table on put/delete and is what makes WATCH work; its doc claims expiry, transactions and **the async queue** all reach the store through it. On the rocks path the queue does not: `write_queue.rs:317` calls `rocks.apply_writes(...)` directly, underneath the wrapper, and the file never receives a `WatchTable` at all. A queued write is therefore invisible to a watcher, and an EXEC whose watched key it changed COMMITS instead of aborting -- a silent wrong answer. Found while fixing the identical hole in ADR-0027's batching, which `proxy_conformance` caught as `EXEC expected NilArray, got Array([OK])` and only THROUGH THE PROXY: on one connection WATCH leaves txn.watches non-empty so the write is not deferred, while the proxy puts it on a different backend connection whose watches are empty. Unseen because --async-writes is opt-in. Fix: give the queue the table and bump every key at commit, BEFORE it -- a spurious bump only aborts a transaction that might have committed, which WATCH permits; a missed bump commits one that must abort, which nothing does. **(FIXED 2026-09-01.** The queue's consumer now takes the WatchTable and bumps every key in the batch immediately BEFORE `apply_writes`, as ADR-0027's `commit_pending` does -- a spurious bump can only abort a transaction that might have committed, which WATCH permits, while a missed one commits a transaction that must abort. Pinned by `a_queued_write_bumps_the_watch_table` as a positive control: removing the bump fails it on the same line. `watch.rs`'s claim that its coverage was total, which NAMED the async queue as reaching the store through the wrapper, is corrected -- that sentence is why this went unnoticed.) |
| BUG-0081 | a benign read timeout rotates the proxy's control-plane seat (defect 1 FIXED, defect 2 OPEN) | The proxy rotates control-plane seats on ANY error from its watch, and `EAGAIN` (`os error 11`) on a subscription socket is a READ TIMEOUT -- the control plane having nothing new to push, which is the normal idle state. On one seat it re-subscribes and logs an error every second (and made a clean `elasticache-bench` run exit 1 after producing every number). On three it would walk the seats once a second forever, paying a fresh CPWATCH and filtered snapshot per rotation on an idle fleet. Invisible to the drills because the data path never consults the watch -- the last-applied table keeps serving by design. The rotation itself was added for a real failure (a proxy pinned to a killed seat); what is missing is "dead" versus "nothing to say yet". (OPEN -- whether the read timeout is deliberate, and whether the three-seat churn is real, are both inferred from reading the loop rather than running it.) **NARROWED 2026-09-02:** the three-seat prediction is no longer an inference. `cp_watch_idle_drill.sh` now stands up THREE control-plane seats, idles past the 30s read timeout, and asserts zero rotations AND that the count of applied filtered snapshots does not grow -- a fresh snapshot per rotation being the cost the prediction was actually about, and a second observation of the same property that survives the rotation log line being renamed. One seat exercises the trigger; only three exercise the WALK. Current code does not churn at three seats, measured. The claim about the PRE-fix code stays a prediction: it is untestable now without a seam, so what is guarded is that it cannot start happening, not that it once would have. Still open: which call produced the observed EAGAIN. |
| BUG-0082 | the rejoin adopts a cursor the archive can no longer serve | A demoted seat restarts, rewinds past the promotion fence, logs `adopted the master's translated cursor`, and then dies with the SAME FATAL as BUG-0079 -- `WALGAP full sync required ... archive/140130.log: No such file ... this link can never resume` -- before serving anything, then quarantines **4957 snapshots** as unresumable. **It is not BUG-0079.** That one needs throughput to consume the archive: 2 TB ingest, 150-200 MB/s, 15 minutes in. Here the load is a 50-116 ops/s soak, the archive is 278 MB against an 8 GiB limit holding a full 12 h, and **all 27 occurrences on this pair (18 + 9) land 8-14 log lines after a `flintctl start`** -- never during steady operation. The sharp discriminator is WHICH TERM PRUNED: RocksDB applies whichever of TTL or size trips first, and `rocks.rs:348` records that 0079 was the SIZE budget ("the term that actually fired"); here size is ~30x away and only the TTL is in play -- same FATAL string, different pruner. LIKELY CAUSE IS THE TRANSLATION, NOT RETENTION: retention is `open_with_retention(ttl, mb)` and NO replica cursor reaches it (`min_acked_live` feeds only the shed gate, which protects the archive indirectly via backpressure) -- so for retention to be at fault the archive would have to drop a needed segment within SECONDS, against a 12 h TTL and a 30x-distant size bound. A correctly-translated cursor after a seconds-long absence sits seconds deep in that window; this one is outside it. The absent-replica hole in `min_acked_live` is real (BUG-0012 closed the lagging-LIVE case) but does not explain these 27. TO SETTLE IT: log the AGE of the adopted cursor and of the oldest retained segment, not the distance -- under TTL pruning a distance looks plausible while the age is absurd. Invisible because it self-heals on the next start, because nothing restarted it (BUG-0077, whose fix was dead 19 h on a mode bit), and because the operations agent has repaired it **16 times in three weeks** with 15 reporting success -- a repair that works is a fault that stops being reported, and the agent's intent journal was the only record it had happened at all |
| BUG-0083 | the build read has one attempt, and every way it can fail looks identical (FIXED) | `proxystats_field` (`crates/flint-ctl/src/main.rs:900`) reads a proxy's build with ONE attempt and no retry, and its `else { return None }` collapses every distinguishable outcome into the same value: connect refused while the seat is still restarting, an unfinished TLS handshake, a per-read timeout, any non-`Bulk` reply including `-NOAUTH`, and a proxy genuinely reporting no build. `roll_edge` treats `None` as FATAL -- **after it has already rolled every seat** -- so one missed read turns a completed roll into `UPGRADE ABORTED ... came up but would not report a build`. When it fired on the public gate at 69c190b the 12-line failure tail carried no timeout, no connection error and no `-NOAUTH`: **the log could not name the cause because the code discarded the class before anything could log it**, which is the defect demonstrating itself. Asked which of two fixes the evidence supports, the answer is neither -- on that run or any future one -- so the first fix is DISTINGUISHABILITY, not retry and not digest ordering. Fourth instance of the shape `edge_roll_drill.sh`'s header records (rc.29, #102, rc.47): three times the roll was right and the reading of it wrong, and each was fixed by making one reading more capable rather than by making it say WHY it failed. Same family as BUG-0081 (a benign `EAGAIN` rotates the CP seat) but a different path -- the build report never touches the CP watch. The split fleet it leaves IS detected, by `half_rolled_drill.sh` -- **but that drill and its gate stage live in the ops repo**, so the managed fleet is covered and every Elastic-2.0 deployment is not. An independent run at the same SHA PASSED (`admin_gated_proxy 34.8s`); given a timing-window mechanism a pass is a run where the window did not open, so greens bound the rate and only a red demonstrates the defect. **FIXED 2026-09-01** across all three tiers -- proxy, control plane and the node roll -- through one classifier answering three ways; retry deliberately NOT added, since which transient fires is still unknown and the next red now names it. Owed gap CLOSED 2026-09-02 by `build_read_failure_drill.sh` in the gate: `FLINT_BUILD_READ_FAIL` arms the read failure, and the drill asserts the abort names the failed read and its reason rather than claiming the seat answered with nothing. Both controls earned their place -- arm A caught a single-member pair that would have made arm B pass against a fleet that could never roll, and arm B first exited 0 because `assert_build` is skipped without `--version-tag` |
| BUG-0084 | a test with no total deadline wedged a release build (FIXED 2026-09-02) | Cutting v0.1.0-rc.67, the release build sat 40 minutes on `migrate::ctl_reply_cap_tests::trickling_peer_is_refused_rather_than_buffered` and would have died with its own 90-minute box. **BLOCKED, not slow** -- both instance status checks healthy and CPU flat at 0.24% across 20 minutes, where one busy core of 16 reads ~6%. That distinction is the finding: the test's own comment warns about "the quadratic re-decode", which makes slowness the expected story and the wrong conclusion. NOT a regression from the cut -- migrate.rs is byte-identical to rc.66, MAX_CTL_REPLY_BYTES is the same 8 MiB, and the test has been in since 49a137b5 (BUG-0060, 08-27). **CORRECTED, and the correction is worth more than the conclusion**: those three runs NEVER EXECUTED THE TEST. `--exact` needs the FULL module path so the filter matched nothing, and the region is behind `#[cfg(feature = "rocks")]` so without `--features rocks` the test is not in the binary at all (102 tests instead of 130) -- and **cargo exits 0 when a filter matches nothing**, so "PASSED in 0s" was a run of nothing, and the 0s should have told me. Re-measured with the full path, `--features rocks`, and a hard kill at 150s so a hang could not pass as patience: original 24s/18s/18s all PASS, with fix 25s/18s/18s all PASS. Conclusion survives -- not a deterministic hang -- and the real figure is ~18s per run, not zero. **FIXED 2026-09-02**: a total deadline the test cannot exceed (the call runs on a worker behind `recv_timeout(300s)` and panics naming the byte cap that should have refused it), plus `set_write_timeout(5s)` on the peer so the writer cannot park forever on a full socket buffer -- the shape that blocks at zero CPU. Costs nothing: 3/3 at the same 18s. The deadline path was exercised by shortening it to 1ms, which is also how the filter bug surfaced, because the control kept reporting success. **The first bound was 120s and it REDDENED MAIN** -- the same mistake twice in one bug, measuring on the wrong machine: 18s on a 16-vCPU laptop is not the number that matters, and the last GREEN ci run warned at 60s and finished ok at ~120s, so the bound sat exactly on CI's normal duration. Read from the actual run timestamps rather than guessed a second time; 300s is ~2.5x measured CI. Shape: the peer thread does `loop { s.write_all(&blob) }` with no write timeout, and the client is bounded only PER READ (`Duration::from_secs(30)`) against a BYTE cap -- "a byte cap rather than a cumulative deadline, deliberately", which is right for the product and leaves the TEST with nothing bounding its total runtime; a writer parked on a full socket buffer waits forever at zero CPU, which is what was observed. Fix shape (not attempted -- rc.67's bytes are frozen and this is pre-existing): give the test a total deadline that names what it waited on; set a write timeout on the peer side; and consider a per-test cap in CI, since nothing but a human watching a release would have noticed before the box self-terminated, at which point it reads as "the build box died" rather than "one test wedged" |
| BUG-0085 | a control panicked on the state it exists to establish (FIXED) | `a_genuinely_recycled_wal_still_raises_walgap` opens with retention OFF and then, before its real assertion, runs a control to prove the scan genuinely cannot reach the stale cursor -- skipping loudly if the environment kept everything, because a test that cannot fail is worse than one that does not run. The control was `get_updates_since(0).expect("iter")`, and **the `Err` it panicked on IS the condition under test**: retention off means the segments behind sequence 0 are deleted, and RocksDB reports that by failing to open one (`IO error: No such file or directory: ... archive/000018.log`) -- stronger evidence the scan cannot reach `stale` than an iterator that opens and yields a newer floor. Found deliberately, by running the same 139-step suite on a **c7i.2xlarge (8 vCPU/16 GiB)** instead of the usual **c7i.4xlarge**: the smaller box recycles sooner, so the segment is gone by the time the control looks. The test was never stable, only lucky in the size of box it habitually ran on; the same commit passed 139 steps on the 16-vCPU box. **FIXED 2026-09-02** as a tri-state rather than a blanket `Err(_) => None` -- a missing segment is the recycling the test needs, and any OTHER error panics with what it was, since collapsing the two would let a broken database walk into the assertion and pass it for the wrong reason (ADR-0028: "cannot look" is not "absent"). The one defect the size change surfaced was a UNIT TEST, not any of the 21 fleet drills, the opposite of what was predicted; box size is a cheap axis of variation that nothing varied. **CORRECTED same day:** this row first reported the half-size box as "1991s against 1955s, 1.8% slower for half the vCPU" and read it as a finding -- a second FULL-size run at this commit totalled 1987s, four seconds from the small box, against a 16-vCPU spread of 32s, so the claimed effect was inside the noise and both baselines were already on disk when it was published. The number that holds came from a QUARTER-size box: 2346s against 1987s, **+18.1%**, build 1.64x and drills 1.06x, with `cp_watch_idle` at 41.6s on both because it sleeps for 40 of them. Measurement and decision: OPS-0107 in the ops repo. |
| BUG-0086 | the port check compares declarations to each other, and never to use | `cp_watch_idle_drill` declared 6521-6523; `controller_multipair_drill` runs pair C as `(6520 6521)` and names both in `--manage-pairs`, so 6521 is a live seat there -- and its `fleet_init` declared `6500 6501 6510 6511 6520`, **not 6521**. Two drills shared a port and nothing said so, because `assert_no_port_overlap` greps `^fleet_init` lines and compares **declarations against each other**: a port used without being declared is invisible to it, and an undeclared port is exactly the case where the declaration is wrong. What makes the declaration load-bearing is `fleet.sh` -- `_fleet_ours` takes a pid if the ps line carries the scope dir OR a declared port -- so here the scope dir still claimed the seat, which is why nothing broke. FIXED by declaring 6521 and moving cp_watch_idle to 6603-6607; both were needed, since declaring alone would correctly turn the overlap check red. **The check gap is OPEN, with the measurement recorded:** scanning the unambiguous `127.0.0.1:PORT` form across 129 drills, six use a port they never declare (backup 6999, build_stamp 7001/7500, config_drift 7999, controller_ha 64512, fleet_guard 7788/7789, txn_failure 6999) -- but **that scan would not have caught this bug**, since 6521 appears only in a bash array and a `6521:$DIR` string, and the looser scan that does catch it also flags ports named in comments and ports chosen BECAUSE nothing listens on them. Shipping it with an exclusion list would reproduce the defect: a list saying "these are fine" is a second declaration to keep in sync with use. **And it is not that nobody thought of it:** `assert_no_used_path_overlap` in the SAME file scans drills for the `$FLINT_DRILL_ROOT/<name>` paths they USE rather than declare, and even carries an ARMED guard that fails when the pattern matches nothing -- "the exact way a duplicate-port check in this suite passed vacuously for as long as it existed". Paths got the treatment; ports never did. Same shape as `assert_no_scope_overlap`, which exists because the port check "was read as covering ownership; it never looked at $2" -- one level down again. **CORRECTED before it shipped:** reading all six candidates, EVERY ONE is right as it stands -- 6999 twice is a `FLINTSLOTFREEZE` destination nothing binds, 7001/7500/64512 are inside comments and log samples, `config_drift`'s 7999 is a CP that MUST be unreachable ("prove the null branch is reachable by taking the CP away"), and `fleet_guard`'s 7788/7789 are a SIMULATED peer drill's ports written into `$PEER.lock/ports` so the guard has someone else's lock to find. Zero true positives; the proposed check would have been wrong six times out of six. So the finding is that **a port literal is not evidence of ownership** -- a port in a drill may be one it binds, one that must stay unreachable, one standing in for another drill, or a number in prose, and scanning cannot tell them apart. That is also why the path check ported and this one does not: `NAME=$FLINT_DRILL_ROOT/<x>` is ALWAYS ownership, with no idiom for naming a directory you need absent. What is constructible is narrower -- scan the arguments that BIND, not port literals: 6521 appears in `--manage-pairs` (binds) and fleet_guard's ports in `--pairs` (connects), and that distinction catches this bug and none of the six. |
| BUG-0087 | `upgrade` re-rolls seats already on the target, and fails over healthy pairs to do it (FIXED for the pair phases) | `fn upgrade` rolls every seat unconditionally -- no build comparison anywhere in it. The canary is rolled whether or not it already holds the target, the replica loop rolls every replica, and the master phase is `for (i, pair) in inv.pairs.iter().enumerate()` with a fenced controlled failover per pair and no check first. Right for a fresh roll; wrong for a PARTIALLY rolled fleet, which is precisely when someone re-runs the command. Re-running to move two straggling seats can cost a failover on every pair in the fleet, each one a real write interruption, taken while an operator is already recovering from a roll that went wrong. Fix is idempotence: skip a seat reporting the target, skip a pair whose master already holds it. Two details worth deciding rather than discovering -- a master that IS behind still needs a failover, because a master cannot be warm-restarted onto a new build without promoting its replica first, so the saving is per-pair not global; and "already on the target" must be read from the live `FLINTINFO build:` stamp, with BUG-0083's rule that a failed READ is not a mismatch. FIXED 2026-09-03: `already_on` reads the live stamp and the canary, replica loop and per-pair failover all skip a seat that holds the target; a failed read rolls rather than skips. The canary's ROLL is skipped and its SOAK is not -- a canary already on the target has proved the build starts and nothing about the window. `roll_edge` is deliberately NOT covered: it is bespoke per seat type rather than a loop, and none of those seats costs a failover, so re-running still restarts up to four edge processes needlessly. Asserted as a differential -- the first run's log must carry the failover line and the second must not, since "skipped" is equally what a run that did nothing would say. Found by checking ADR-0036's own claim that finishing a stalled roll was "one `flintctl upgrade` away" before writing code that depended on it -- it is not |
| BUG-0088 | `failover` fails when one write in 20 000 projects past the SHIPPED write deadline (FIXED 2026-09-05) | Two gate runs two days apart failed identically: `THROTTLED write would wait ~2017ms, past --write-deadline-ms 2000` (and ~2033ms), with `errors: 1, replies: 20000` -- one write in twenty thousand, landing 0.85% and 1.65% past the line. **The 2000 ms is `DEFAULT_WRITE_DEADLINE_MS` (`flint-server/src/main.rs:1000`), the shipped default**; `failover_drill.sh` never overrides it, so the threshold crossed is the one every deployment runs. The comment setting that default predicts waits "sub-millisecond at any sane concurrency" and asks to be revisited against a measured p99 curve -- none exists, and this is the first evidence of where the tail sits: 2017 ms on plain `ubuntu-latest` at `FLINT_GATE_JOBS: 4`. **Not BUG-0035**, which is the lag cap (`writes_shed_lag`); this is the write-deadline path (`main.rs:3993`) that projects a wait and refuses before queueing -- same `-THROTTLED` to the client, different knob. It IS a counter-example to that file's "LINUX does not shed at any burst" framing: this is Linux, on loopback, shedding. Not caused by the commit it fired on -- `422a1bde` fired the same way two days before `ae51c612`, which touches no write path. **NOT established: whether it is new.** Of 60 failed gate runs since 08-16, 18 were examined; `failover` failed 3 times, all on 09-02/03, two with this shed. The other 42 are unexamined and pre-08-20 logs have aged out. **And the distribution is unknown, because the wait is recorded ONLY when it exceeds the deadline** -- both samples are above the line by construction, and whether a passing run sits at 200 ms or 1990 ms is a different bug in each case. So the first fix is the instrument: print the max projected wait on every run, which turns 60 future gate runs into the p99 curve the default was always supposed to be checked against -- the same gap, and the same fix, as BUG-0014's tie counter that printed only when non-zero. Do NOT raise the deadline to make it green: that converts a measurement into a silence | **2026-09-04: four firings now carry the packed factors, and they split.** `failover` crosses on DEPTH (inflight 218-244, service 8-9ms — a healthy seat offered more than it can take); `restart` crosses on SLOWNESS (inflight 30-65, service 31-67ms — the seat itself got slow, 4-8x the other drill's). The product is identical to within 9ms because 2000 is where the refusal happens, so every sample is pinned just above it by construction; what varies EIGHTFOLD is which factor got it there. So this is not a failover bug — two drills, one shipped default. **Superseded 2026-09-05, FIXED: the deadline was never the problem.** `est_ms = inflight x service` charged each member of a batch for the whole shared commit, and `inflight` was batch occupancy capped at `MAX_PURE_BATCH`=256, not queue depth — so the DEPTH half of "two regimes" dissolves and the readings above were the instrument, not the system. Corrected by Little's Law; the 2000 ms is unchanged. The SLOWNESS half stands and is the open question: why a commit on the `restart` path takes eight times what it takes on `failover`. See the file.
| BUG-0089 | a failed bring-up is not fatal, and its reason is discarded (FIXED) | `fleet_wait_ping` **exits** when a seat never comes up; `fleet_wait_listen`, in the same file, **returned**. The drills run under `set -u`, not `set -e`, so a failed listen printed its line and execution CONTINUED into the assertions below — dying on whichever tripped first, a claim about the PRODUCT for what was really a seat that never started. Measured: **131 unchecked `fleet_wait_listen` calls across 60 drills**, and **77 bring-up spawns across 31 drills** discarding stderr with `2>/dev/null &`. Two functions side by side, one returning and one exiting, reads as two contracts rather than one oversight, and neither half is visible from a passing run. FIXED in the library: `fleet_wait_listen` exits like its sibling, and all three failure paths (`wait_listen`, `wait_ping`, and `wait_ready`'s both branches) call `fleet_why_not_up`, which prints the non-empty logs in `FLEET_SCOPE` and, when there are none, says the reason was **discarded rather than absent**. **Done 2026-09-04, and the deferral reason was wrong:** every drill that spawns a seat has already called `fleet_init`, so every one has `$FLEET_SCOPE` — all 77 write `${FLEET_SCOPE}<name>.log`, which is where the helper looks, and no per-drill path was needed. A concurrent re-count of **54** is the same work at a narrower scope — 54 seat binaries, plus 14 control planes and 9 proxies this pass also converted — and the **8 spawns with NO redirect stay as they are**, since their stderr is inherited by the drill and already reaches the gate artifact. Two defects surfaced doing it: `fleet_why_not_up` tested `[ -d "$FLEET_SCOPE" ]` when the scope is a PREFIX (`${FLEET_SCOPE}.lock` is a sibling file), so it found nothing every time and then blamed the spawn — asserting a cause it had not established, inside the function written to stop that; and nothing cleared the logs between runs, so a re-run would have reported the PREVIOUS failure's output as today's cause, which `fleet_init` now prevents. Superseded note: ** In the ops repo that conversion was mechanical because every drill there names its scratch dir `$D`; here it varies per drill (`MDIR`, `RDIR`, `mktemp -d $FLINT_DRILL_ROOT/…`), so each needs its own log path and its own reading — 31 files of careful edits, worth doing and not worth doing blind in one pass. Found by porting the ops repo's OPS-0117 and asking whether the same class lives here; it does, three times over. `b7b74a9` had already fixed the neighbouring class (23 drills discarding `bootstrap` output) after tracing six red gate runs to seat bring-up being the largest and least diagnosable failure cluster — this is the seat-spawn half of the same finding |
| BUG-0090 | the write timeout that fixed a hang made the test blame the product (FIXED) | `test (rocks)` passed one gate run and failed the next on the IDENTICAL tree, faster because the build was warm and failing because it was faster: `expected the byte cap, got: closed`, `UnexpectedEof` where `InvalidData` belongs. `UnexpectedEof` has one source — the peer thread returning — and BUG-0084's `set_write_timeout(5s)` had given it one, because `write_all(..).is_err() -> return` cannot tell **the client hung up** from **the client is slow**, and a full socket buffer is what a slow client looks like. The fix for one hang manufactured a flake in the test written to catch it. Proven, not inferred: at a 1ms write timeout the failure reproduces BYTE-IDENTICALLY. Natural rate NOT measured (1 failure, then 16 isolated and 4 full-suite runs green) — mechanism established, rate not, and those are different claims. Fixed by retrying on `WouldBlock`/`TimedOut` with `write` and an offset (a whole-blob retry after a partial write would splice a truncated bulk and fail for a third reason), a real error still ending the thread, and a 300s deadline so BUG-0084's forty-minute park stays impossible. The 1ms control is kept as `a_slow_reader_does_not_look_like_a_missing_cap`: the sibling asserts the cap fires, this asserts the harness cannot counterfeit its absence. Mutation control passed — cap defeated, suite still red. BUG-0064's shape a third time, and the third candidate running where a test-harness timing assumption, not the product, was what risked stopping the cut. |
| BUG-0091 | four core files carry the ops repo's license header (FIXED) | This repo is Elastic-2.0 and **220 files under `tools/` and `crates/`** say so via SPDX; four drills instead carry `Copyright (c) 2026 Crestway AI LLC. All rights reserved.`, which is the correct line in the **ops** repo and the wrong one here — a public tree telling a reader that four of its files grant nothing. Found by making the identical mistake while adding `tools/collection_read_peak.py` and catching it against the convention. **Deliberately not fixed in passing:** Elastic-2.0 is a grant, so rewriting the line hands out rights the current text withholds, which is outward-facing and Jeff's call, not a cleanup to infer. Four lines to fix if the history is what it looks like (a paste from the sibling repo). The repo has no header check, which is how four drifted; the fix is worth pairing with a gate step comparing the two counts, which cannot be flaky. |
| BUG-0092 | `rewind_rejoin` greps the rejoin decision before the seat has made it (FIXED) | A gate run failed `arm E — a quarantined snapshot was not reconsidered under a LOWER fence`; the drill passes locally, repeatedly. Every arm spawns the rejoining seat, calls `fleet_wait_ready`, and greps the log on the NEXT line — but `fleet_wait_ready` returns when the LOCAL load finishes, while the rewind-versus-reseed choice is a REPLICATION event logged after the seat contacts its master. BUG-0064's family, fourth instance this week after `decommission` (which reddened the rc.68 release gate), `cold_start_roles` and BUG-0090. **The worse half is arm F**: it asserts the same marker is ABSENT, which an unwritten log satisfies, so it has been passing whenever it lost the race and proving nothing about BUG-0062's livelock staying closed — a visible flake and an invisible pass from one defect, and the flake is the lucky half. Fixed with `wait_rejoin`, which blocks until the seat logs ONE of its three decisions (waiting for a specific marker would be wrong: three of six assertion groups are about absence) and leaves every existing grep as the verdict. `fleet_wait_log` already existed for exactly this and this drill was not among the eight sites it was written for. |
| BUG-0093 | the WAL archive's size budget is only enforced every 10 minutes (ACCOMMODATED — volumes sized for the overshoot, 120 GB at 200 MB/s) | `rocks.rs:347` says RocksDB applies whichever of TTL/size trips first. The size term can only trip when a purge pass RUNS, and the pass is throttled to `min(kDefaultIntervalToDeleteObsoleteWAL=600, ttl/2)` — so at our shipped 12 h TTL the byte budget is evaluated once every **600 s** and the archive is unbounded between passes. Measured: 64 MiB written into a 4 MiB limit grew the archive linearly to 64 MB with the shipped TTL and **never pruned once**, while `ttl=1` pruned repeatedly and held 7–39 MB; `wal_archive_mb` reported 4 in both. At 142 MB/s that is ~85 GB accumulating past an 8 GiB budget before anything is deleted, which is the assumption BUG-0079's volume-derived sizing rests on. Also explains why a size-bound window cannot be tested quickly: a short TTL gives fast passes but then ages files out by TIME, so the two knobs cannot be separated. An earlier 16 MiB version of the measurement looked like it REFUTED this and was simply underpowered — the pruning arm's sawtooth peak sits near 16 MB. |
| BUG-0094 | `tenant_rebalance` asserts conservation while a slot move is still in flight (FIXED) | `FAIL: alpha lost/gained keys: 15000` against a seeded 12000 — the only one of that batch of gate failures that reads as a CORRECTNESS claim rather than a timing one. The settle predicate is satisfiable before the plan finishes: `rebalance EXECUTE` is printed by `execute_move` BEFORE the first unit moves and the units then migrate SERIALLY, and fills after TWO of the three alpha units are g0=10000 g1=6000, where `10000*100 <= 8000*125` holds EXACTLY on the boundary. `DBSIZE` counts residency, the proxy SUMS the masters, so 12000 + one in-flight 3000 = 15000. **The sibling `rebalance_execute_drill.sh` already found this and says so in a comment** — "a race in the ruler, not a defect in the product" — and the fix never propagated, the same shape `gates.sh` already names for controlplane/lease. Sub-second on an idle laptop (six local runs passed silently); paced with `--migrate-rate-bytes 20000` it is deterministic — **31 of the 32 samples satisfying the old predicate read 15000**, the new one fired once, reading 12000. Polling `FLINTMIGRATIONS` is NOT a sufficient signal: the destination clears `Importing` before the flip and the source's `Moved` is hidden from the bare form, so for the length of the source's purge both nodes report nothing in flight while both still hold the rows (BUG-0025's shape). Settles on units instead — every unit in `units [...]` has a `MIGRATEIN-OK`, no node reports importing/migrating, fills in deadband — deliberately NOT the sibling's `SUM == TOTAL`, which would make the wait and the assertion the same predicate and leave conservation unfalsifiable. Second instance fixed in the sibling: its `MOVES -lt EXECS` guard compares completed UNITS against plan ANNOUNCEMENTS at `--max-slots-per-cycle 2`, so it holds only while the in-flight unit is the first of the first cycle — a normal passing run reports `executes: 2, slot moves: 4`, so any timeout after cycle one fell through to accusing the product of duplicating rows. |
| BUG-0095 | a field that reads `none` vanishes from Prometheus exactly when it matters (FIXED) | `lag_ms`, `seq_lag`, `acked_seq`, `cert_days_remaining` and `disk_free_pct` render the word `none` in one state each — no live replica, unreadable cert, unreadable filesystem — and `flint-exporter` emits only fields that `parse::<f64>()`, so those series are PRESENT while healthy and ABSENT in the state the alert was built for. Found by diffing a live FLINTINFO against the parse, not by reading the format string: the `none` is in the binding. `wal_headroom_seq` answers the identical question with `-1` and survives the pipeline, so the same file solves it two ways. FIXED with the sentinel: `-1` for the four unsigned fields, `-99999` for `cert_days_remaining`, whose real range includes -1. Guarded by `flintinfo_numeric_drill.sh`, which a node in its DEFAULT state is enough to run |
| `0096-a-write-deadline-shed-reddens-the-restart-drill-as-if-data-were-lost.md` | one refused write in 101,000 failed a durability drill (estimator pinned at the 2000ms deadline while inflight x service moved 2x); and separately `verify_after` raced the convergence it asserts, so `flintctl bootstrap` could exit 1 on a healthy cluster whose replica was still loading (both FIXED) (FIXED 2026-09-05) |  |
| `0097-the-gates-self-check-reads-gates-sh-as-text-and-a-correct-refactor-broke-three-of-its-reads.md` | 4bcba0c made msrv opt-in and three of gates_drill.sh textual reads of gates.sh broke: ALL_STAGES read as literal text reported two VARIABLE NAMES as stages, `want [a-z_]+` matched `local want scanned rest` and called `scanned` an undeclared stage, and an exact-literal STAGES= match stopped forcing the copy. Each now reads for the value or the role, never one spelling (FIXED) |  |
| BUG-0098 | the proxy forwards the one reply that means "retry me" (FIXED) | `flint-server` answers `-LOADING` while its dataset loads and justifies the spelling on the grounds that "every mainstream client library already retries" it -- but Flint's clients reach the PROXY, whose header states its reason to exist as absorbing exactly this class within the retry budget. It absorbed MOVED, TRYAGAIN and READONLY and forwarded this one verbatim: the arms enumerate CODES and nobody added the fourth. `grep -c LOADING crates/flint-proxy/src` returned 0. Reachable on every re-seed (the ordinary tail of an unattended AttachReplica), every revived ex-master, every rolling restart. Fixed at three sites: `forward` and `forward_collect` absorb it (rediscover, back off only when the re-probe still points here -- BUG-0055's rule -- and do NOT drop the connection, since a loading node's socket is healthy and only its dataset is not); `call_pinned` ABORTS it inside a MULTI, joining the other three for a different reason and the same outcome -- a node that is not queueing cannot hold a transaction, and passing the reply through would show a client `-LOADING` where QUEUED belonged and then EXEC a transaction missing that command. Found while confirming BUG-0041 and explicitly NOT that bug: both of its surviving occurrences recorded the proxy's own no-master string. **The test's three false starts are the write-up's real content** -- a wrong bulk length, `-LOADING` answered to the pool's own HELLO handshake (which the capability assert passed, because a failed handshake still counts as a command seen), and a bare block_on where the write path needs a LocalSet |
| BUG-0099 | `node-env` accepts a name no seat reads, and says nothing (FIXED) | `node-env K=V` is the only way to reach the engine's compaction knobs on a managed fleet -- flintctl spawns the seats and the operator never touches a command line -- and the parser accepts any non-empty key. `FLINT_BG_JOB=4`, one character short, is accepted, forwarded to every seat local and remote, read by nothing and reported nowhere: the operator changed a knob in their head and not on the fleet, and the only symptom is that the tuning does not appear in the numbers. Found reading BUG-0013's proposal, which spent three weeks measuring these knobs and ends with a pairing an operator applies per seat -- every application goes through this parser, and a silent typo yields the BASELINE number, which looks like the tuning was tried and failed. Fixed with a WARNING (not a refusal -- node-env is an escape hatch by design, and a list that is a snapshot of another crate must cost a spurious line, never a rejected command), `FLINT_`-prefixed only. **A pairing warning was drafted and DROPPED**: BUG-0013 says "never one alone", but the same file measures `bg_jobs=4` alone at **+93%** at 96 GB, so the rule would have fired on a configuration its own evidence supports. `node_env_names_match_the_seat` scans the seat crates' source and fails in both directions, mutation-checked. Its capability assert earned its place on the FIRST run: the scan resolved to a path that does not exist, read zero files, and would have certified any list |
| BUG-0100 | `stop` reports a host it never reached exactly as it reports a clean one (FIXED) | `Runner::output` returns `Err` only when the ssh BINARY cannot be spawned, so an ssh that never reached the host -- unreachable address, refused key, no remote flintctl -- is `Ok` with status 255 and no stdout. `stop` looped over the (empty) stdout and printed nothing; `sweep_orphans` had no error arm at all and `unwrap_or(0)`'d the count, folding *no orphans*, *could not ask* and *unparseable* into the same 0, under a summary that only prints `if swept > 0`. So `stop` against a fleet with an unreachable host printed `no pidfiles -- sweeping by process instead`: what a clean fleet prints. Its own opening comment names the invariant broken -- reading only the local directory "would leave remote seats running while reporting success". Both arms now check the STATUS and say what is unknown, with the remote stderr's last line, because "exited 255" alone sends the reader to the wrong machine. Held by `host_verbs_drill.sh` against TEST-NET-1, mutation-checked one per arm -- the second mutation reprints the bug verbatim. |
| BUG-0101 | nine ADR numbers name two different decisions, and one citation lands on the wrong one (OPEN) | This repo's ADR README states the invariant: "They are numbered in one sequence on purpose... renumbering per repository would make the two halves impossible to discuss together." It is broken nine times -- 0016, 0018, 0019, 0023, 0024, 0025, 0026, 0027, 0028 each name a DIFFERENT decision in each repository. Eight are latent (each repo's code cites its own). **0023 is not**: `crates/flint-storage` cites `ADR-0023 D7`/`D7.1` SIXTEEN times meaning the managed-plane ADR whose D7 is "Flint must gain an evictable namespace class", while this repo's ADR-0023 is `slot-aligned-bulk-eviction` and **has no D7 at all**. A reader follows the citation, opens the ADR-0023 they have, and finds a document that is ALSO about eviction, looks right, and does not contain the thing cited -- worse than a dangling reference, because the README's answer to dangling ones (the call-site comment states the decision) only works if the reader knows to fall back on it. NOT a complaint about citing private ADRs, which is stated policy and a good one; the defect is two documents sharing one number under a rule that says numbers are global. Remedy recorded, not taken -- renumber (costs 53+60 citations and unrewritable links), qualify the citations (cheap, fixes the live case, leaves the rule broken), or retire the shared-sequence rule -- because it is a numbering DECISION. A collision check belongs in the gate that has both trees, but would need a grandfather list for nine pre-existing cases, which is the shape BUG-0086 argues against: build it after a remedy so it starts at zero (CLOSED 2026-09-05 — the decision already existed: the managed plane's ADR-0030 accepted `OPS-ADR-<n>` on 2026-08-27, on this file's own reasoning, and its Status line said `proposed` for nine days afterwards — so a settled question was re-derived from scratch. Status corrected, this repo's adr/README now states the accepted rule with the nine-number table, and the sixteen live `ADR-0023 D7` citations are qualified as `OPS-ADR-0023`. The other eight collisions and the 68 bare cross-repo citations here are left alone because ADR-0030 makes adoption incremental; a gate check still cannot start from zero and is recorded rather than built.) |
| BUG-0102 | three more remote call sites read a failed ssh as an answer (FIXED) | The BUG-0100 audit, run over every `Runner::output` call site: `spawn`, `mark_reseed`, `stop_seat` and the push-bins unpack were already correct; `seat_alive`, `kill_pidfile` and the statedir `mkdir -p` were not. `seat_alive` is the one with teeth -- every caller reads its `false` as "spawn one", and an ssh that failed gave empty stdout, no pids, false. The call sites' own comments say what that costs: "the duplicate gets a lost port race and a clobbered pidfile -- after which every stop aims at a corpse". It now answers TRUE when the host cannot be asked -- and because that made `bootstrap`'s post-PING diagnosis assert "its PROCESS IS RUNNING" about a host that does not resolve, the probe is split out as three-valued and the diagnosis names three causes. Only the statedir arm is reachable from one machine, which is a fact about the code and is stated rather than left implied. |
| BUG-0103 | the matrix promised every command was gated by the oracle, and a 1000x unit error passed it (FIXED) | `docs/command-support.md` opens by telling customers every supported command is gated by a conformance case run against both engines. Nothing in `gates.sh` or any drill read that file, so the sentence had never been evaluated -- and it was false for six commands: `PEXPIREAT`, `PEXPIRETIME`, `COMMAND`, `FLINTKEYSIZE`, `FLINTKEYSTAMP`, and `FLUSHALL`'s effect. The sharp case is the expiry pair: they share an implementation with their second-granularity twins and differ by ONE argument, the unit multiplier (`cmd_expire_at(args, "pexpireat", 1)` against `1000`). **Measured, not reasoned about**: both multipliers swapped to 1000 and the corpus passed 105/105 with all 105 server unit tests green -- a one-hour TTL becoming a thousand hours, invisible to everything the repo runs. The obvious test does not catch it: a `PEXPIREAT`->`PEXPIRETIME` round trip passes unchanged with BOTH multipliers swapped, because the second conversion undoes the first, which is also the likeliest mutation since one hand edits both lines. Every assertion in the new case crosses the units instead, and all three mutations now die at a named step. Two more defects fell out of the same audit: `HKEYS`/`HVALS` were in a case's NAME and in none of its steps -- the name is what the run summary prints, so a coverage audit read from the output counted them -- and were missing from the matrix entirely despite being served since the first release, so a reader would have written the more expensive HGETALL-and-discard workaround; and `QUIT` is BUG-0106. Fixed by `assert_every_dispatched_command_is_gated`, which reads the dispatcher's arms and the corpus's steps. **No exemption list**: a command with no oracle (`COMMAND` answers an empty array where Valkey answers with its whole table) still gets a case in a `flint_only` family, because an exemption list is the shape BUG-0086 argues against and the alternative cost four small cases. It cannot pass by reading nothing -- it anchors on the match SUBJECT (`name_upper.as_slice()`), which is what separates a command arm from an option arm (an earlier draft keyed on indentation and read `b"COUNT"`, a SCAN option, as a command), and if that anchor is restructured away it FAILS with "read no commands at all" rather than reporting agreement between two empty sets. Both failure modes confirmed by planting them; reports 125 dispatched, 133 exercised. Scope stated in the failure text: it covers `commands.rs`'s dispatcher, and a hit establishes that no corpus step names the command, NOT that the command is untested |
| BUG-0104 | the mem build reports a replica field under a name nothing reads (FIXED) | `FLINTINFO` from the rocks build emits `live_replicas` holding a COUNT; the mem build emitted `live_replica` holding a 0/1. Different name and different units, from two renderers, one per build. Nothing read the singular -- that string without a trailing `s` appeared in exactly one place in the repository, the line that produced it -- while every consumer reads the plural: `flint-controller` parses `"live_replicas" => ... unwrap_or(0)` and `promotable()` requires `live_replicas >= 1`, so a controller fronting mem seats would have found NO promotable node, ever, with every seat healthy and every log quiet. Latent, not live: production runs rocks, and of the twelve tools passing `--engine mem` only `gates.sh` also starts a controller, where the mem seat is the conformance target rather than a controller-fronted node. Fixed by emitting the plural and the count; no compatibility cost because there was no consumer of the old name. Held by the `flint` family's `FLINTINFO` case, confirmed to fire by restoring the old spelling. One wrong reading first: the mem run passed while `target/release` held a binary built with `--features rocks`, and a rocks-featured binary started with `--engine mem` still takes the ROCKS renderer -- eighty fields printed, proving nothing about the code just changed. The mem-only path is reached only by the mem-only build. Same family as BUG-0028: the binary on disk is not necessarily the one the source says |
| BUG-0105 | `pipeline_nodelay`'s positive control cannot be armed on macOS, so the drill is a permanent local red (FIXED) | Arm B deliberately skips `TCP_NODELAY` and requires the round trip to exceed 20 ms, because arm A's 0.2 ms proves nothing unless the slow case can be produced. On darwin 25.5 a 32-deep 1 KiB pipeline still round-trips in 0.3 ms with Nagle left on, so the condition never occurs and the control cannot be armed. Reproduced serially with nothing else running, and reproduced identically on a clean HEAD with the tree stashed -- not a regression, and nothing in the product is wrong. The drill is behaving CORRECTLY by failing: it refuses to report a pass it cannot substantiate, which is the whole point of a positive control. What is missing is any statement that this is expected here, so the red reads as a product failure every time. That matters more than a skipped test would: a local `drills` run cannot go green on this platform, and a gate that is permanently one-red is a gate people learn to read past -- the next real failure arrives in a list that already had a failure in it. Three remedies recorded and none taken, because the choice is about what the local gate is FOR: detect the platform and skip LOUDLY (cheapest, but a quiet skip becomes a hole on the platform where the seam is most likely edited); find a load that does stall on darwin (only option that keeps coverage everywhere); or declare it Linux-only and run it on the gate box, already the default for a core gate |
| BUG-0106 | `QUIT` works while a seat is LOADING and stops once it is READY (WONTFIX) | A READY seat answers `ERR unknown command 'QUIT'`; the same seat while loading, and the proxy in front of it, answer `+OK`. So the verb works during startup and stops working once the node is actually serving -- backwards from any expectation. The only arm is in `serve_loading` (`flint-server/src/main.rs:3269`); the ready path routes everything into `commands.rs`, which has no `QUIT` arm, so it falls through to unknown-command. `QUIT` IS in that file's `NO_KEY` list, which is precisely the trap the matrix already warns about: `NO_KEY` says a routing slot cannot be derived from argument 1 and says nothing about whether anything implements the verb. Not fixed here because `QUIT`'s contract is reply-then-close, the dispatcher returns a `Value` and cannot close a stream, and the place that can is `serve`'s batching loop -- closing there without flushing a pending batch would drop writes a client believed were in flight, trading a durability bug for a compatibility nicety. Client impact is small: clients reach seats through the proxy, which answers correctly. `docs/command-support.md` listed `QUIT (minimal, compatibility-shaped)` unqualified and now states where it is answered. No conformance case pins the current seat behaviour on purpose -- a case asserting `ERR unknown command` would make the defect the contract and force the eventual fixer to delete a passing test |
| BUG-0107 | the retry-safety page omitted 37 writes, including every pop but `LPOP` (FIXED) | `docs/retry-safety.md` is what a client reads to decide whether re-sending a command after an ambiguous failure is safe. It classified 25 of the 62 commands `is_write_command` calls writes, said nothing about being partial, and nothing checked it. The omissions were the data-losing ones: the page warns that `LPOP`/`RPOP` "pops an EXTRA element -- silent data loss on retry" and did not mention `SPOP`, `ZPOPMIN`, `ZPOPMAX`, `LTRIM`, `ZREMRANGEBYRANK` or counted `LREM`, every one of which does exactly that. A reader looking up SPOP, seeing LPOP flagged two lines away and SPOP absent, draws the reasonable and wrong conclusion. Every classification added was RUN against a live seat, and two came out opposite to the obvious reading: `LTRIM 1 2` twice on `a b c d` leaves `c`, and `ZREMRANGEBYRANK Z 0 0` twice removes two members -- both read as absolute-range deletes and are position-addressed, so from first principles they would have landed in the SAFE list, which is worse than omission. Their value-addressed twins were run too and do converge (`ZREMRANGEBYSCORE`, `ZREMRANGEBYLEX`, `LREM key 0 m`, `LSET`, `SETRANGE`). The page now leads with the rule that fell out of it -- a retry is safe when the command names WHAT to change, unsafe when it names WHERE, HOW MANY or HOW MUCH -- which covers pops, trims, counted LREM and every INCR-shape in one sentence and lets a reader classify what the table omits. It also names a second hazard shape it had only described for `SET NX`: five commands land the write and then report failure on the retry (`SETNX`/`HSETNX` 0, `COPY` without REPLACE 0, `GETDEL` nil, `RENAME`/`RENAMENX` and `BF.RESERVE` an error), which is worse than a visible error because both natural responses are wrong. Held by `assert_every_write_command_is_retry_classified`, which reads the classifier the proxy and server already share rather than a second list that would drift. It establishes PRESENCE, not correctness -- a command in the wrong table still passes, stated in the failure text so the check is not trusted for what it does not do. Both failure modes confirmed by planting them |
| BUG-0108 | the command matrix denied a capability two other pages tell you to use (FIXED) | `docs/command-support.md` said "There is no substitute for `CONFIG` or `SHUTDOWN`". `FLINTCONFIG` is a substitute for CONFIG: no args dumps the live tunables, `<key> <value>` hot-reloads one with no restart (wal-fsync-ms, lag-soft-ms, lag-hard-ms, wal-headroom-seq, min-replicas-to-write, max-conns, migrate-rate-bytes, fullsync-rate-bytes, write-deadline-ms, gc-sweep-ms). Two other customer-facing pages already said so -- slo.md and space-reclaim.md both write "hot-settable via FLINTCONFIG" -- so three pages disagreed and the authoritative one was wrong. This is the expensive direction: an operator who believes it takes a RESTART, an outage on that seat, to change a value that needs none. Four more commands the guides tell readers to run were absent from the matrix entirely: FLINTNSBYTES (space-reclaim.md, deciding whose data to trim), PROXYSTATS (self-hosting.md), PROXYLATENCY and PROXYHOTKEYS -- the last two addressed to TENANTS in tenant-guide.md ("your hot keys -- no portal required"), on a page that says of itself "if you are deciding whether Flint implements something, the list to consult is Supported above". A page claiming to be the authority has to be one. Fixed by correcting the CONFIG sentence (and recording that it was wrong, where a reader of the old one will see it), and by adding an Operator and per-tenant commands table naming all five plus what is deliberately NOT listed: FLINTPROMOTE, FLINTFENCE, FLINTLEASE, FLINTSYNC and the rest are control-plane machinery that flintctl and the controller drive. Held by `assert_guides_only_name_documented_commands`, which classifies every docs/*.md as a GUIDE (nine) or INTERNALS (three: architecture, capacity-model, failover, which name CPLEASE/CPWATCH/FLINTDEMOTE/FLINTMIGRATEIN/FLINTSLOTHEAT/FLINTSLOTSTATS as mechanism) and FAILS if a page is in neither -- which is what keeps it from being the shape BUG-0086 argues against: a new doc cannot be quietly uncovered, and a renamed or deleted one fails rather than dropping out. Whether a token is a command comes from the server and proxy sources, not a doc heuristic, which invented three findings during BUG-0103. All four failure branches confirmed by planting them |
| BUG-0109 | Flint left private-key modes to whichever openssl minted them (FIXED) | `docs/security.md` stated `certs/int.key` is mode 0600 as a fact Flint enforced. Nothing in the repository set a file mode -- grepping crates/ for set_permissions, PermissionsExt and from_mode returned ZERO matches -- so the mode came from the TLS toolchain. **CORRECTED 2026-09-06, and the first version of this row was wrong in the expensive direction.** It claimed a world-readable CA key "on every deployment shape" and rated the bug high, written from a developer laptop plus a correct reading of the code path, without checking a single real host. All three live boxes -- playground, flint-ops, flint-ops-b -- carry `-rw-------` keys and always have, so NO deployed fleet was exposed and there is no operator action. What decides it is the implementation: Amazon Linux's OpenSSL 3.x restricts a generated private key to 0600, LibreSSL 3.3.6 (the system openssl on macOS) leaves it at the umask, 0644. `ca.crt` is 0644 on those same hosts, so it is not a restrictive root umask doing it. The real defect is narrower and still worth fixing: a security property was left to whichever openssl happened to be installed, and on macOS -- every developer machine, and any LibreSSL host -- keys minted by a drill or a local bootstrap were world-readable, `ca.key` included. `cert_manifest` declares `("int.key", "600")` and its only consumer, `push_certs`, opens `if !r.is_remote() { continue; }`, so even that mode reached only copies pushed to other hosts, never the originals, and on a single-host fleet nothing at all; ca.key is absent from the manifest entirely because the CA key is pushed nowhere. Fixed by `harden_key_modes`, 0600 on every `*.key` and 0700 on the certs dir, from mint_certs and resign_leaves so bootstrap and rotate-certs are both covered, with a chmod failure FATAL rather than a warning. Held by a unit test starting from files deliberately created 0644 and by `cert_reload_fleet_drill.sh` asserting both directions -- keys 600 AND certs 644 -- after bootstrap and again after rotation. Confirmed by removing the fix: "ca.key is 644, want 600" and three more |
| BUG-0110 | the self-hosting quickstart cannot work as written (FIXED) | Copied verbatim it dies at `bootstrap`: "proxy 0.0.0.0:7379 (dialled at 0.0.0.0:7379) never answered PROXYSTATS within 10s". Every seat starts; the proxy is fine. `proxy 0.0.0.0:7379` is a wildcard BIND, and with no `proxy-host` the dial target is the bind address verbatim -- so flintctl validates the edge cert against the name `0.0.0.0`, which is not among its SANs (`IP:127.0.0.1, DNS:localhost`), and sends BadCertificate. Deterministic and platform-independent. Nothing caught it because every other inventory in the tree binds loopback -- README.md's own quickstart and every drill say `proxy 127.0.0.1` -- and the fleet harnesses that DO bind a wildcard also declare `proxy-host`, as a multi-host fleet must. Quickstart now binds loopback, verified end to end through SET/GET; the wildcard-plus-`proxy-host` variant verified too. Held by `assert_doc_inventories_are_runnable`. The error message still blames the proxy for what is a certificate rejection its own log knows about -- filed separately, because that is a diagnostic and this is a document. |
| BUG-0111 | omitting one inventory line gave a plaintext mesh the security page said could not exist (FIXED) | `docs/security.md` said "there is no plaintext internal hop to turn off, and no 'trusted network' assumption". There was one, and turning it off took REMOVING a line rather than adding one: `tls` is a `bool` on a `#[derive(Default)]` struct, so it is false unless the inventory says `tls on`, and `internal_args` returns an EMPTY vec when it is off -- seats spawn with no --internal-ca, no --internal-cert and no --internal-key, so replication, migration, cutover, proxy-to-backend, proxy-to-control-plane and the Raft RPCs between CP seats all run in plaintext with no peer authentication. No deployed fleet was affected: `render-inventory.sh` emits `tls on` and `client-tls on` unconditionally under a comment saying the posture is NOT parameterized, and every production inventory comes from it. But that is the shape `require_release_or_disposable` already argues against one guarantee over -- "a guard you can step around by invoking the tool directly is a convention, not a guard" -- and the TLS posture was a convention that held because nobody hand-wrote a production inventory. Fixed by `require_tls_unless_disposable`: bootstrap refuses an inventory with neither `tls on` nor `disposable on` and says what to add. Scoped to bootstrap because that is where the posture is chosen and the CA minted, and because gating every mutating verb would strand a pre-guard fleet (`stop` is mutating; refusing to stop a plaintext cluster helps nobody). `disposable on` is exempt for the same reason the release guard exempts it, and because all 88 drill inventories are exactly that shape -- a guard that broke every drill would be turned off rather than satisfied. The decision is a pure predicate so all four combinations are asserted without a fleet, and all three live paths were run against a RELEASE-TAGGED build, which was necessary: FLINT_RELEASE_TAG is baked in at compile time, so a first attempt with it set at runtime hit the release guard instead and proved nothing about this one |
| BUG-0112 | the README dismissed a 21% gap using a spread that does not cover it (FIXED) | Every throughput figure in the front page's "Our numbers" matches `docs/bench/2026-09-01-published-tables-re-measured.md` exactly -- both tables, all ten rows -- and the methodology paragraphs are honest, including admitting the RTT probe failed to build on the beyond-RAM fleet. The numbers were right; the sentence telling readers what they mean was not. It said GETs, Mixed and SETs are all "within this rig's run-to-run spread -- the two SET runs behind that row differed by 11% between themselves, which is wider than the gap to the older figure". GETs are 5.6% under the 2026-08-17 figure and Mixed 6.0%, both inside 11%. **SETs are 20.7% under** (69,340/s against 87,449/s), nearly double the spread -- and the SET row is the one the sentence cites as its evidence. A reader doing the subtraction gets 21% against a justification that says 11%. The stronger evidence existed and was left behind: the bench doc explicitly does NOT rest on the spread, saying "a cross-day comparison cannot distinguish a code change from a different afternoon; the same-fleet A/B can, and that is what the conclusion rests on", and reports that A/B -- this build against pre-batching, three reps each at depth 1, writes within ~2% and reads within 1%. So the private measurement reasoned correctly and the public page kept the weaker half: not a wrong number, but a DOWNGRADE in the quality of the argument as it moved from bench doc to README, in the direction that made the conclusion look easier than it was. Fixed by stating the 21% gap plainly, saying the spread does not cover it, and attributing the conclusion to the A/B. No published figure changed because none was wrong. Still not recorded: the two individual SET runs behind the 11% are absent -- `docs/bench/raw/` holds per-run logs only to 2026-08-17 -- so that number cannot be checked by anyone in either repo; noted rather than fixed, since the runs are gone and re-running answers a question the A/B answered better |
| BUG-0113 | the tenant guide promised no quota ever blocks a read, and the ops/s quota does (FIXED) | `docs/tenant-guide.md` -- the page tenants themselves read -- stated as an invariant: "You can always read your data out. No quota state ever blocks a read." Two lines above, the same page defines `-THROTTLED` as "back-pressure: your ops/s quota, a loss-protection guard, or admission control", so it calls the rate limit a quota and then says no quota gates a read. The rate limit gates reads: `Topology::quota_gate` takes `is_write`, uses it for the STORAGE cap only (`over && is_write && !reduces_space(name)`), then falls through to a token bucket that never consults it. The proxy's own unit test settles that this is intended and not a slip -- it asserts a GET passes once and is THROTTLED on the second call in the same instant. Severity medium-low because the behaviour is transient, retryable, and the page already says to retry -THROTTLED, so nobody is stuck; what was wrong is a stated INVARIANT, the kind of sentence a client stops designing around. What was right and stays stated: the storage cap never blocks a read (`over && is_write` is exactly that, and is the invariant the sentence was reaching for), and the self-clear path is never shed (space-reducing writes are exempt via `reduces_space`, so a full tenant can always delete their way out). A second smaller gap in the same table: the exemptions were listed as DEL/UNLINK/FLUSHALL/EXPIRE where `reduces_space` covers seven, adding PEXPIRE, EXPIREAT and PEXPIREAT. Under-promising and harmless alone, but it collides with advice this repo now gives elsewhere -- retry-safety.md tells clients to PREFER the absolute forms EXPIREAT/PEXPIREAT wherever a retry is possible (BUG-0107), so a tenant following that advice while over quota was using a command the guide did not list as exempt. Fixed by scoping both invariants to the storage cap, giving the ops/s quota its own paragraph saying plainly that it paces reads and space-reducing commands alike and is a rate rather than a lockout, and completing the exempt list. Nothing in the product changed |
| BUG-0114 | the cert-mode assertion asserted the author's umask (FIXED) | Two defects sat on top of each other in `cert_reload_fleet`, the single red step in a 137-step box gate that passed locally. The FIRST -- `stat -f '%OLp' \|\| stat -c '%a'`, macOS-first, where GNU `-f` means FILESYSTEM status, takes `%OLp` as a filename and exits 1 with the block already captured by `$( )`, so no comparison could ever match -- was found independently by a peer session the same afternoon and pushed first under BUG-0109. Their ordering ships. The reasoning is kept here because it is where it lives and because the second defect is unreadable without it: a fallback chain is only sound when the wrong tool ERRORS rather than succeeding with a different meaning, and BSD stat rejects `-c` while GNU stat `-f` reinterprets `%OLp`, so exactly one of the two orderings can work. THIS BUG IS THE SECOND. With the mode finally readable the cert arm still asserted the literal 644, and 644 is a umask: the certs are 664 on the gate box (umask 002) and 644 on macOS (022), both correct, because `harden_key_modes` clamps only `*.key` and its own unit test asserts "certs must NOT be clamped". So the assertion was about the machine the drill last ran on, and the peer's fix -- correct about the ordering -- keeps the literal and stays red on any umask-002 host. Replaced with the property a clamp would break, which is what the check was added for: a certificate must stay readable beyond its owner or every dialer verifying against it stops. `[ $(( 0$m & 0044 )) -ne 0 ]` -- 600 fails, 644 and 664 pass. Five controls re-run against the MERGED check, their loop with this property, because the merge is what ships and neither half had been exercised in that form: both umask shapes pass, a cert clamped to 600 is caught, a key widened to 644 is caught, and a stubbed `stat` that fails both ways is REFUSED rather than compared. Nothing in flint-ctl changed -- the code was right throughout. Both sessions went to the box because the box is the only place the defect is visible, and neither knew the other was there; the cost was one duplicated 25-minute gate run |
| BUG-0115 | "the proxy never answered" could not say the NAME was wrong (FIXED) | `bootstrap` dies on a proxy that is running: flintctl validates the edge chain against the name it DIALLED, and the cert does not carry `0.0.0.0`. The message offered three causes -- a held port, a crashed proxy, a missing `edge-trust` -- and the real one was not among them, though it already had both facts in hand: it printed the trust path and the dial address and never read the leaf. `flint_tls::cert_sans` now renders the names a leaf is valid for (`127.0.0.1`, not `IPAddress([127, 0, 0, 1])`) and the message says "the edge certificate carries [...] and flintctl dialled `x`". Three outcomes, not two: an unreadable leaf says the cause is UNKNOWN rather than staying silent, since absence would read as "the name was fine". Verified on BUG-0110's reproduction, with the arm that matters being the healthy fleet where the new text must NOT appear. |
| BUG-0116 | the capacity model sized a cluster on a constant halved a month earlier (FIXED) | `docs/capacity-model.md` is the page an operator sizes a cluster from, and three errors compounded, all optimistic. **(1)** It said `poll 200 ms x confirm 3 => detection ~0.6 s`; `--poll-ms` has defaulted to **100** since 2026-08-02, dated in the controller's own comment along with the measurements behind it (7 EC2 hosts, 30 kills per setting: 200:3 gave p50 644ms, 100:2 gave 317ms, the shipped 100:3 lands ~430ms). Not overridden anywhere -- flintctl's inventory doc says `poll-ms 100` and the ops renderer emits `${FLINT_POLL_MS:-100}`. The table contradicted ITSELF and nobody subtracted: two rows up it gives the client-observed failover outage as ~472 ms, and detection cannot be 0.6 s inside a 0.472 s outage it is ~70% of -- the two rows describe the same phenomenon at two vintages. **(2)** The detection formula was `confirm x max(poll, sweep)`, which makes any sweep under the poll free. The loop sleeps THEN sweeps, both loops sequential (pairs in turn, nodes in turn), so the period is `poll + sweep`: every millisecond of probing adds rather than hiding. **(3)** A dark node was priced at 800 ms, which is `call`'s read/write timeout and applies only after a connection exists; an unreachable host costs `TcpStream::connect_timeout(.., 3s)`, 3.75x more, while a REFUSED connection costs nothing. Corrected arithmetic moves the rule from <=32 pairs per shard to **<=24** and the v1 cluster from 64 pairs to **48**, both labelled as re-derivations rather than measurements. Also removed a claim that never followed from its own formula: "even 8 simultaneous dark nodes keep detection inside 3x poll" -- eight cost 6.4 s hung or 24 s unreachable, so no poll value and no shard size makes it fit 600 ms; that one was wrong on 2026-08-02 too, independently of the stale constant. Why it went unread: nothing checks this file, and it is the one doc whose numbers are all constants living in code |
| BUG-0117 | the duplicate-seat guard was inert on a multi-seat control plane (FIXED) | `launch` refuses to spawn a CP seat that is already there, and a dial cannot decide it: a Raft seat replaying its log answers nothing until ready, so PING failing means "not serving", not "absent". The second check is a process probe, and it passed the LITERAL `<statedir>/cp-state` for every seat. `pids_in_ps` matches an ident as a whole TOKEN, and a three-seat CP spawns with `--state <statedir>/cp-state-n1`, `-n2`, `-n3` -- so the ident matched NONE of the three and the probe answered "no process" for three live seats. That is the dangerous direction, because every caller reads false as "spawn one": the guard against duplicating a still-replaying seat was inert on exactly the topology it was written for, and the single-seat CP where it works is the one that cannot have the problem. The comment at the call site says what a duplicate costs -- "a lost port race and a clobbered pidfile, after which every stop aims at a corpse". Nothing caught it because one seat spells the dir `cp-state` (so the literal is right there) and every inventory under packaging/ renders exactly one `cp` line, while the two three-seat drills bootstrap, kill the leader and assert a mutation lands without ever asking flintctl to bring a seat BACK -- which is where the probe runs. Fixed with `cp_seat_state(inv, i)` at both call sites, the respawn decision and the timeout diagnostic that was reporting "process absent" on the same wrong ident; and `cp_seat_args` now calls `cp_seat_state` rather than repeating its rule, since the probe matches the `--state` token the spawn writes and two copies of one rule stop agreeing when one is edited. Three unit tests built from PARSED inventories: the literal finds zero of three seats while each per-seat ident finds exactly one, the single-seat path is unchanged, and spawn-vs-probe spell it identically for 1 and 3 seats -- the last confirmed by reverting the fix. WHAT THEY DO NOT COVER, stated: nothing here fails if `launch` goes back to a literal, because a unit test cannot easily observe `launch` deciding to spawn; that guard is fleet-level, a duplicate CP process being countable on the host. CORRECTED the same day, once that exercise existed: `packaging/aws/cp-quorum/run.sh` (ops) does count them and passed, and it would have passed on the UNFIXED code too -- the seat it kills is genuinely dead, and there the broken probe and the fixed one agree (absent, spawn, correct). The harm needs a seat that is up but not answering, a Raft seat replaying its log, and nothing opens that window yet. So the honest coverage is: token semantics and the two spellings pinned by unit tests, the ordinary restart path guarded on a real two-host fleet, and the defect's own condition never reproduced -- found by reading, fixed by construction |
| BUG-0118 | the proxy's own header said the tenant boundary had no TLS (FIXED) | `crates/flint-proxy/src/main.rs`'s module header listed six things as "v0 scope, deliberately deferred: TLS, metering, cross-slot scatter-gather, hot-key absorption, RESP3, inline commands". FOUR OF THEM HAD SHIPPED. Client-facing TLS is `--tls-cert`/`--tls-key` building a ReloadableServerConfig snapshotted per connection so rotation needs no restart, drilled by chaos_edge_tls, edge_ca_trust and edge_roll. Metering is `commands_read_total`/`commands_write_total` in PROXYSTATS, which the agent's metering.rs bills from and billing_reconcile_drill holds at 0.00% drift. RESP3 is negotiated per connection with HELLO, and command-support.md has a section on why it is not cosmetic (redis-py 8 defaults to it and sends credentials inside the handshake). Inline non-RESP commands are parsed under `MAX_INLINE_LEN`, `verify` asserts one is accepted, and client_compat covers them. Only cross-slot scatter-gather (`key_of` returns the FIRST key) and hot-key ABSORPTION (the sketch ships and "never toggles behavior") genuinely remain. It matters more than a stale comment usually would because it IS the tenant boundary and it said it had no TLS: a self-hoster reading the source to decide whether tenant traffic is encrypted gets the wrong answer from the file that implements it -- and the repository contradicts itself, since command-support.md documents RESP3 as supported. Third instance of one shape this week, after ADR-0030's Status line and the M4 lane's remote runner: a state claimed in two places and maintained in one, rotting in the place where nothing checks it. Surfaced by auditing the roadmap's M3 line (`metering/scatter-gather/per-tenant quotas remain`, when per-tenant quotas have two drills) and chasing it back to the source. The method is worth keeping: a "deferred" claim is testable, one grep per noun against the code that would implement it -- six nouns, ten minutes, four wrong. Header now names the two that remain with where to verify each, and records the four that shipped rather than dropping them silently; the roadmap line is corrected in the same change. NO CHECK IS PROPOSED and the write-up says why: the honest general form needs to understand prose, and a keyword sweep would be a list nobody reads |
| BUG-0119 | flintctl panicked on a master that was merely mid-promotion (FIXED) | Found by the M2 failover soak's first long run, at kill 449 of 800: 448 kills had completed cleanly, RTOs 550-600ms with zero acked writes regressed, and then `restart-node` panicked with "pair 0 has no reachable master" -- while the status block taken moments later reported `172.31.78.236:7002 master epoch (0,214)`. There WAS a master; it was mid-transition when the question was asked. flintctl had FIVE places that look for a pair's master. Four did a single-pass find over the members and panicked if neither answered `role: master` at that instant. The fifth, the cold-start probe, runs the SAME check inside a 40 x 250ms budget and then DEGRADES rather than panicking, with a comment saying why: a member booting into a full sync is alive and not yet serving. One path had learned that "no master right now" is a transient and four had not, and a promotion takes hundreds of milliseconds, so a single probe is a coin toss weighted by how recently something failed over. Nothing caught it because the affected verbs -- add-replica, swap-node, decommission-node, migrate-slots -- are drilled on a QUIET fleet, and a 12-kill chaos run is over before it is likely to ask during a transition. At a kill every ~3 seconds, 449 in, it is ordinary: the soak doing the one thing it exists for, and the argument for it being a soak rather than more drills. Fixed with `master_of`, shared by all four, carrying the budget the fifth already used -- and returning Err rather than panicking, because a panic in an operator tool prints a backtrace hint and no remedy. The refusal now names every member and the role it answered, including `<unreadable>`, and says that a promotion may be in flight and re-running is the right response: ADR-0028's obligation, since the old text named a CONCLUSION and nothing it observed, leaving an operator unable to tell a pair that is gone from one asked about during a failover. Two tests pin the message without needing a fleet; the retry itself is not unit-tested and the write-up says the soak is its test |
| BUG-0120 | the loss-depth measure was anchored to a stamp taken before the kill was sent (OPEN) | The M2 soak's 800-kill run reported 80 acked keys regressed and, on the same line, `(all within the 1000ms cap)`. **That parenthetical is a string literal**, printed unconditionally beside the count; it renders identically if every loss is an hour past the cap. Discount it and two measured quantities remain, both zero -- and both anchored to `kill_ms`, which is stamped at main.rs:509, twelve lines BEFORE `kill_master_hot()`. On `Target::Attached`, the path a real fleet uses, that call makes a master-discovery round trip and then an SSH kill before stamping `dead_us`: measured windows of 711ms, 720ms and **3304ms** on exactly the three kills that lost anything. A write acked inside that window was served by a master still alive, so `kill_ms.saturating_sub(at)` floors to 0 however deep it truly was, and `at <= kill_ms - 1500` never fires. `deepest acked-write loss: 0ms` was the saturation floor, not a clean measurement -- and the summary then printed `NOTE: loss depth 0 means replication kept up throughout` on a run that had just lost 80 acked keys. THE READING IS RECOVERABLE, because the run prints the size of the only escape hatch: `boundary ties: 2`, two entries excluded as unattributable out of eighty. So the population was measured, every loss really did have `at >= kill_ms`, and each one's true depth is bounded by its iteration's kill->death window -- which settles **56 of the 80 within the cap** (iters 610/645, windows 720/711ms) and leaves **24 unsettled** (iter 603, 3304ms). Fixed by anchoring both measures to `dead_us`, which is stamped just after the SIGKILL returns and so OVER-estimates depth: it can call a loss deeper than it was, it cannot call a deep loss shallow. Per-iteration depth replaces the constant; the depth-zero NOTE no longer claims replication kept up on a run with losses. The removal of the age ASSERTION is not reopened and was right -- the product bounds volume, not age -- but the comment justifying it promised the depth was still measured, and the measurement could not see the window where loss happens. A SECOND defect underneath the first: the loss COUNT is `got < last_acked`, over every ack recorded, while the loss DEPTH is measured inside a loop that skips `Served::NewMaster` -- a send stamped after the death. `classify_send` has three outcomes; `Ambiguous` is skipped AND counted (BUG-0014, correctly), `NewMaster` was skipped and counted by nothing. So count and depth judge different populations, and the difference is the most SERIOUS case, not the most benign: a write acked after the old master died and then missing from the survivor is the NEW master failing to hold what it acknowledged, and it was inheriting the reassurance the depth measures earn for everything else. Now counted as `post_death_surplus` and `lost_unattributed`, both printed every run including zero, with a note that the RPO numbers say nothing about those keys. This qualifies the verdict above: the 56 covers losses attributable to the OLD master and is a floor on what is explained, not a complete account, since batch 3 does not record the residue. Sweeping for consumers of the changed strings found the SAME shape twice more, both fixed here: `tools/lag_cap_drill.sh` printed `no shed write was ever counted as acked (... all within the cap)` with no assertion on either half, and its `${LOST:-0}` default would have rendered a moved summary line as a clean zero -- replaced with two capability asserts and a measured statement; and `cluster.rs`'s comment explained every recorded `0ms` by loopback speed "including a 7-host run over a real network", which is the one case where the anchor, not the speed, produced the zero. Not caught because `Target::Local` kills a child directly, so the two stamps are microseconds apart and every chaos drill in CI is local: sound on the topology it is tested on, inert on the topology it is for. STILL OPEN: iter 603's 24 keys need a re-run on the fixed instrument, and the VOLUME bound the product actually promises is asserted nowhere |
| BUG-0121 | the direct-path RTO measured the SSH hop and called it recovery (reporting FIXED; the exit verdict OPEN) | The M2 soak failed its exit on RTO -- worst 6002ms against a 3000ms budget, 4 of 401 master kills over -- and the slow promotions were not slow. Across all 401 kills: reported RTO p50 572ms, and the KILL DISPATCH (`kill_ms` to the SIGKILL returning) p50 **712ms**. The dispatch p50 EXCEEDS the reported RTO p50, because the direct-path figure is `first post-outage ack - kill_ms` and `kill_ms` is stamped BEFORE the kill is dispatched -- on a multi-host fleet a master-discovery round trip plus an SSH hop. The number is dispatch plus recovery, dominated by the former. THE OBVIOUS REPAIR IS WRONG and the data says so before any argument: anchoring to `dead_us` yields NEGATIVE recovery times (p50 -144ms), because `dead_us` is stamped when the SIGKILL RETURNS, after the signal landed. So kill_ms is too early, dead_us too late, and the death sits inside a ~712ms window neither stamp resolves -- the same anchor family as BUG-0120, which was the loss DEPTH; one harness, two headline numbers, both measured from a stamp that is not the event. The honest number already existed and was discarded: `max_stall_ms`, the worst gap between consecutive ACKS, needs no kill stamp and is what a client experienced. The EDGE path already reports it as its RTO (through the proxy a client sees no error, so calling it RTO would overstate the outage); on the direct path the writer computes it identically -- not edge-gated -- and nothing ever surfaced it, and the soak runs direct. Fixed by printing both components per-iteration (`dispatch=34ms client_stall=25ms`) and in the summary, with a NOTE that neither stamp bounds the failover. Verified locally where dispatch is small and the decomposition still shows: RTO 42ms against dispatch 34ms + stall 25ms. NOT SETTLED: whether batch 3 actually breached -- the stall was measured that run and not printed, and the log holds only aggregates, so a re-run settles it in one pass. And the reported RTO and its assert are deliberately UNCHANGED: judging the exit on the dispatch-inflated figure is judging it partly on SSH latency, but moving a milestone's measurement is a decision, not a fix |
| BUG-0122 | the client stalled ~3.1s after a promotion that finished in ~0.5s (FIXED — confirmation pending) | SETTLED by the instrument it asked for, on that instrument's first run. soak-20260908T020420Z (800 kills, 421 promotions) breached once, and the line carries the answer: max_connect_ms=3000 with max_hold_ms=4 and client_stall=3221ms, where 3221 = 3000 + the run's ordinary 221ms recovery. THE DIAL BURNED THE CONNECT TIMEOUT. Conclusive rather than suggestive because the dial distribution over 421 kills is bimodal with nothing between: 9473 dials failed after a kill and 9472 cost about a MILLISECOND (a dead port answers RST at once), while exactly one cost the full 3000ms -- a latency problem makes a tail, this makes a cliff, and the one point at the cliff is the one breach. The journal agrees as before (Detected +467ms, Promoted +474ms), so the fleet had a writable master ~3s before the client found it. The seat prediction also came true and is now barely needed: the victim dialled FIRST carries the only dial over 29ms (n=211), the other's worst is 29ms (n=210), medians 266 vs 264ms. CAUSE: the harness restarts the killed seat immediately and dials it first; for a narrow window the SYN goes UNANSWERED rather than refused, and the dial pays flint-tls's CONNECT_BACKSTOP in full -- behaviour BUG-0123 measured directly against TEST-NET-1. Note this does NOT vindicate the mechanism I drafted and refuted (a restarting seat binding before it can serve): #176 binds first on purpose and answers, and the real window sits BEFORE the listener is up. Being right about the phase did not make the story about the phase right. FIXED by Jeff's decision of the two remedies raised: the harness's dial is now bounded at DIAL_BUDGET=500ms via the connect_within BUG-0123 added. 500ms comes from the measurements and not from the budget it has to clear -- the worst LEGITIMATE dial across ~9500 was 29ms, so 500 is ~17x it and a two-endpoint walk stays under 1s; 1500ms (this client's read timeout, the symmetry the proxy uses) was rejected because two blackholed endpoints would cost 3s again. This removes the HARNESS's contribution to the measured RTO and changes nothing about the product -- no promotion got faster -- so the next soak must be read as the same fleet with one artefact removed, not as failover improving. Confirmation pending. Three runs, zero breaches attributable to the product's failover |
| BUG-0123 | the proxy bounds the reply and leaves the dial on a backstop (FIXED) | Following BUG-0122 into the path that actually carries customer traffic. `discover_master` sets an 800ms read timeout on the FLINTINFO reply and dials with `connect_reloadable`, which falls through to flint-tls's 3s CONNECT_BACKSTOP -- so one node's worst case is 3800ms against a stated 800ms, and it walks every member of the pair: 7600ms. This is NOT a background refresher; `refresh_pair_master` calls it from inside client request handling, on the Err arm of a backend call, which is the failover path -- reached exactly when a pair member is least likely to complete a handshake promptly. The 3s was never a latency budget: flint-tls's own comment says it replaced MINUTES for a blackholed peer and names proxy back-ends as a caller that cannot afford an unbounded wait, so it is a ceiling on a pathology that a caller with a considered 800ms silently inherited for the phase before it. ALSO CORRECTS BUG-0052, itself long since closed, which analysed this same function and put the two-member probe at 1600ms: that is the reply phase only, the real figure is 7600ms, and the ratio against REDISCOVER_DEBOUNCE is 15.2x per node rather than 3.2x. 0052's single-flight fix stands -- its arithmetic does not. That is now three places (max_hold_ms, 0052's 1600ms, this) where a bound claiming to cover an operation left the connect phase out. MECHANISM DEMONSTRATED: connect_within against TEST-NET-1, which drops SYNs, spends its budget to the millisecond -- 251ms on 250ms, 2001ms on 2000ms, both TimedOut -- which supplies the reachability BUG-0122 could not establish, though NOT that this fleet produced the condition. NOT CLAIMED: no client has been measured stalling 3s through the proxy. The defect is that the stated bound does not cover the operation, which is wrong when it is loose and not only when it is exceeded. Fixed by giving the dial a budget of its own (`connect_within`), so the per-node cost is one constant readable from the code rather than derived from two files |
| BUG-0124 | the journal records that the controller promoted, never why (OPEN) | After BUG-0122 removed the harness's dial from the measured RTO, one kill in 391 still breached the then-3s exit and looked nothing like the previous mode: max_connect_ms=0 with a kill dispatch of 3533ms against a p50 of 723ms. Joining all 391 master kills to the retained journal: detection latency p50 567ms, p95 641ms, worst 3452ms, and Pearson r(detection latency, kill dispatch) = 0.984 -- the five slowest detections are the five slowest dispatches in the same order. On the breach the master stopped acking at ~+307ms and was not detected until +3452ms. THE RULE THAT EXPLAINS IT: should_promote has two paths that differ by an order of magnitude -- a master that REFUSES is a dead process and promotes after confirm ticks (~300ms at --poll-ms 100 --confirm 3), while a master whose socket still ACCEPTS but whose app cannot answer is alive-but-slow and is held for --slow-promote-ms, default 4000ms, deliberately, so a CPU-starved or compacting master is not flapped away from. socket_open says so in place. When a host stalls, all four observations move together -- master stops serving, the SSH hop carrying the kill hangs, the controller keeps seeing an open socket, everything completes at once on recovery -- which is the 0.984 coupling, and THE PRODUCT IS BEHAVING AS DESIGNED THROUGHOUT; the patience is the feature. THE DEFECT IS THAT NONE OF THAT IS RECOVERABLE FROM WHAT A RUN LEAVES BEHIND: Detected records that the controller decided and not which path fired, nor the streak against confirm, nor when slow_elapsed started -- so 'the slow path was taken' stays an INFERENCE from a correlation plus a comment in the source, and the controller's own log carries only PROMOTED with no inputs. Same shape as BUG-0122 one layer down, where two runs were spent arguing about a phase nothing measured and one added measure answered it on the first outing. Fix: carry the path, the streak and the slow-state age on Detected. SIDE EFFECT: M2's exit asked RTO <= 3s while slow-promote defaults to 4000ms, so the exit was unreachable whenever a host stalled -- the published SLO (10s) was never in conflict, only the milestone's exit, resolved 2026-09-08 by moving that exit to 10s |
